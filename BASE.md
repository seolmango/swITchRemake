# swITch

**이 문서는 1.0.0을 정의한다.** 지금은 베타이고, §15.1의 항목이 전부 채워지는 날이 1.0.0이다.

## 0. 이 문서

**이 문서 하나가 전부다.** 코드 밖에 다른 설계 문서를 두지 않는다. 여러 문서로 나누면 같은 결정이
두세 군데에 적히고, 그중 하나만 고쳐지는 순간 어느 쪽이 진실인지 아무도 모르게 된다.

### 버전이 여럿인 이유

한 제품에 버전이 다섯 개 붙는다. 각각 다른 질문에 답하기 때문이다.

| 버전 | 무엇을 뜻하나 | 언제 올라가나 |
| --- | --- | --- |
| 제품 | 사람에게 보이는 이름 | 출시 단위. 지금 목표가 1.0.0이다 |
| 빌드 | 이 바이너리가 무엇인가 | 배포할 때마다 |
| 프로토콜 | 클라이언트와 서버가 같은 말을 하는가 | 주고받는 형식이 바뀔 때 |
| 규칙 | 그 경기가 어떤 밸런스였나 | 밸런스 파일이 바뀔 때 |
| 시야 판정 | 그 리플레이의 개인 시점이 그때와 같은가 | 시야 판정이 바뀔 때 |

- **제품 버전은 저장소 전체에서 하나여야 한다.** 지금은 루트가 1.0.0인데 클라이언트가 0.0.0이라
  서로 다른 말을 한다. 출시 산출물이 전부 같은 값을 달고 나가야 한다.
- 프로토콜이 다르면 접속을 거절한다. 다만 **모르는 조각은 건너뛴다**(§6.2) — 이 둘은 층이 다르다.
  버전이 맞아야 붙을 수 있고, 붙은 뒤에는 서로 모르는 항목이 있어도 죽지 않는다.
- 규칙과 시야 판정 버전은 경기 기록과 리플레이에 함께 찍힌다. 나중에 "그때 그 경기가 어떤
  규칙이었나"를 물을 수 있어야 하기 때문이다.

**이 문서가 요구사항 원본이다. 코드가 여기에 맞춘다.** 코드와 이 문서가 다르면 코드가 틀린 것이다.
코드 쪽이 옳다고 판단되면 이 문서를 먼저 고치고 그 결정을 남긴다.

이 문서는 **무엇을, 왜**를 적는다. 형식의 정의는 코드가 원본이다 — 바이너리 스냅샷의 바이트
배치, JSON 메시지 이름, 제어 명령 payload는 `shared/`의 타입이 유일한 정의이고, 이 문서는 그
계약이 **지켜야 하는 성질**만 적는다. 바이트 표를 문서에 옮겨 적으면 그 순간부터 썩는다.

### 수치를 이 문서에 적지 않는 이유

밸런스에 영향을 주는 값은 숫자로 적지 않는다. 두 군데 적으면 갈라지고, 갈라지는 순간 이 문서가
거짓말을 시작한다. 규칙만 적고 값은 파일에 둔다.

| 종류 | 파일 |
| --- | --- |
| 클라이언트도 알아야 하는 밸런스(도움말·데모가 쓴다) | `shared/src/protocol/tuning.ts` |
| 서버만 아는 밸런스 | `server-game/src/config/gameplay.ts` |
| 전송·부하·상한 | `server-game/src/config/network.ts` |
| 환경(포트, 주소, serverId) | `server-game/src/config/infrastructure.ts` |
| 경험치와 레벨 곡선 | `shared/src/protocol/progression.ts` |
| 보존 기간 | `server-match/src/retention/retention.settings.ts` |
| 시야 판정 상수 | `shared/src/visibility/core.ts` |

밸런스 파일이 바뀌면 `rulesVersion`도 바뀐다. 진행 중인 방은 시작 시점의 값을 끝까지 쓴다.
프로세스를 재시작하지 않고 경기 도중에 전역 값이 바뀌는 일은 없어야 한다.

이 문서에 숫자가 남아 있는 것은 **밸런스가 아닌 값**뿐이다. 인원 제한, 보존 기간, 길이 제한처럼
바꾸면 게임이 아니라 제품이 달라지는 값이다.

## 1. 기본 철학

항상 다음 세 가지를 확인한다.

1. 해커나 악의적인 사용자에게 공격받을 수 있는 부분이 있는가
2. 트래픽을 절약할 수 있는가
3. 알고리즘이 최적화되어 있는가

LLM은 사용자가 명시적으로 요구한 내용이라도 위 철학에 위배되면 이를 알리고 다시 확인받는다.

셋이 충돌하면 **보안 > 트래픽 > 편의** 순이다.

이 문서의 문장은 두 종류다.

- **규범** — 점검 대상이다. "그렇다/아니다"로 답할 수 있어야 하고, §16의 체크리스트가 이걸 센다.
  "안전하게 만든다" 같은 판정 불가능한 규범은 두지 않는다.
- **방향** — 만드는 사람이 판단할 때 쓰는 기준이다. §12.1의 디자인이 대표적이다. "둥글둥글하고
  가득 찬 느낌"은 두 사람이 같은 합격 판정을 내릴 수 없지만, 그렇다고 없애면 화면이 제각각이
  된다. 방향은 점검에서 합격·불합격을 매기지 않는다.

애매하면 규범으로 쓴다. 방향은 그렇게 쓸 수밖에 없는 것에만 남긴다.

# 게임

## 2. 게임 규칙

게임은 최소 3명, 최대 8명이 진행하는 술래잡기다. 각 플레이어는 1번부터 8번까지 고유한 번호를
가지며, 이 번호는 대기실에서 바꿀 수 있다.

이 게임이 추구하는 경험은 **아수라장**이다. 옆에 있던 도망자가 갑자기 술래가 되고, 내가 술래가
됐다가 다시 도망자가 된다. 예측할 수 없는 상황에서 순간적인 판단을 요구받는다. 점점 좁아지는
맵에서 빠르게 쫓고 쫓긴다.

### 2.1 승패

- 술래는 항상 1명, 나머지는 도망자다.
- 술래에게 잡히면 그 경기에서 탈락한다.
- **생존자가 2명 이하가 되면 경기가 끝나고, 그때 살아 있는 전원이 공동 승리자다.** 등수는 없다.
  한 틱에 두 명이 동시에 잡히면 승리자가 1명일 수 있다.
- 술래는 잡히지 않으므로, 경기가 끝나는 순간 술래인 사람은 승리자에 포함된다. **의도한 것이다** —
  스위치와 자동 교체 때문에 술래 지위는 언제 넘어갈지 모르는 불안정한 자리라, 끝까지 술래로
  남았다는 것 자체가 운과 실력의 결과다.
- 3인으로 시작하면 첫 탈락과 동시에 경기가 끝난다. 정상 동작이다.
- 경기 중 퇴장으로 생존자가 2명 이하가 되어도 같은 규칙으로 끝난다.
- **경기에는 최대 시간이 있다.** 도달하면 그 시점의 생존자 전원이 공동 승리자가 되고 경기가
  끝난다. (시간: 밸런스 파일)

  정상적인 맵이라면 자기장이 다 줄기 전에 경기가 끝나므로 이 상한에 닿을 일이 없다. 그래도
  두는 이유는, 닿았을 때 방이 영원히 안 끝나기 때문이다 — 자기장이 플레이어 지름보다 좁아지면
  모두가 한 점에 몰린 채 술래가 못 잡는 상태로 굳을 수 있다. 맵을 잘못 만들었거나 밸런스를
  잘못 고쳤을 때 그 방 하나가 프로세스 자원을 영구히 붙잡는 것을 막는 안전망이다.

### 2.2 술래와 아웃 판정

- 술래와 도망자가 **그 틱에 충돌하면** 도망자가 아웃된다. 충돌을 해결해 둘을 떼어놓은 뒤의
  거리로 판정하면 아무도 잡히지 않는다.
- 무적 시간은 없다. 한 틱에 여러 명이 동시에 아웃될 수 있다.
- 아웃된 플레이어는 즉시 관전으로 전환한다. 방에서 나가지는 않는다.
- 최초 술래는 경기 시작 시 무작위로 정한다. 무작위는 리플레이가 재현되도록 경기 seed에서 뽑는다.
  `Math.random()`이나 현재 시각을 쓰면 같은 경기가 두 번 다르게 재생된다.
- 일정 시간 술래가 바뀌지 않으면 살아 있는 도망자 중 한 명이 무작위로 새 술래가 된다. 경기가
  정체되는 것을 막는 장치다. (시간: 밸런스 파일)

### 2.3 스킬

플레이어는 슬롯 2개를 가진다.

- **이동기**: 아래 셋 중 하나를 대기실에서 고른다. 술래든 도망자든 항상 쓸 수 있다.
  1. 유체화 — 일정 시간 이동속도가 빨라진다. 벽을 넘을 수 없다.
  2. 점멸 — 일정 거리를 순간이동한다. **벽을 넘을 수 있다.** 착지점이 벽 속이면 진행 방향으로
     밀어 바깥으로 내보낸다. 뒤로 되돌리면 "썼는데 제자리"가 되어 불쾌하다. 애매한 상황은 쓴
     사람에게 유리하게 푼다.
  3. 탈진 — 사용 즉시 자신에게 짧고 얕은 감속을, 사거리 안의 **모든** 플레이어에게 더 길고 깊은
     감속을 건다. 진영을 가리지 않아 도망자가 도망자를 느리게 만들 수 있다. 개인전이므로 의도된
     설계다.
- **스위치**: 도망자에게만 활성화된다. 술래가 되면 이 슬롯은 잠긴다.

세 이동기는 서로를 대체하지 않아야 한다. 유체화는 같은 시간에 점멸보다 멀리 가되 벽에 막히고,
점멸은 벽을 넘되 거리가 고정이며, 탈진은 광역이되 자신도 느려지고 쿨타임이 가장 길다. 이 관계를
깨는 밸런스 변경은 하지 않는다.

**자신의 상태가 바뀌면(도망자→술래, 술래→도망자) 스킬 쿨타임이 사라진다. 즉시 다시 쓸 수 있다.**
남은 쿨타임을 다시 채우는 것이 아니라 0으로 만드는 것이다. 역할이 뒤집힌 순간 아무것도 못 하고
서 있는 시간이 있으면 아수라장이 성립하지 않는다.

### 2.4 스위치

도망자가 번호키를 눌러 사용한다. 두 조건이 모두 맞으면 술래가 바뀐다.

1. 현재 술래가 시전자의 사거리 안에 있을 것
2. 누른 번호가 **자신도 술래도 아닌, 살아 있는** 플레이어의 번호일 것

성립하면 기존 술래는 도망자가 되고 지목당한 플레이어가 술래가 된다. 시전자는 도망자로 남는다.
지목 대상까지의 거리는 상관없다 — 맵 반대편에서 방심하던 사람이 갑자기 술래가 되는 것이 이
스킬의 전부다.

- 조건이 안 맞아 실패해도 쿨타임을 소모한다. 아무 데나 눌러보는 것이 이득이면 안 된다.
- 죽은 번호, 빈 번호, 자기 번호, 술래 번호는 모두 실패다. 서버가 판정하며, 클라이언트가 보낸
  번호를 그대로 배열 첨자로 쓰지 않는다.
- 같은 틱에 두 명이 스위치를 쓰면 처리 순서가 결과를 바꾼다. 순서는 번호 오름차순으로 고정한다.

### 2.5 효과

값은 전부 밸런스 파일에 있다. 여기 적는 것은 언제 붙고 무엇을 하는가다.

| 효과 | 언제 | 무엇 |
| --- | --- | --- |
| 광란 | 술래가 된 직후(경기 시작·강제 교체·스위치 지목), 스위치를 성공시킨 시전자 | 일정 시간 이동속도 증가 |
| 광란 누적 | **광란이 붙어 있는 동안** 아웃시키거나 스위치를 성공시킬 때마다 | 지속시간과 증가량이 함께 늘어난다 |
| 피곤 | 스위치에 당해 술래에서 도망자가 됐을 때 | 일정 시간 이동속도 감소 |
| 탈진(자기) | 탈진을 쓴 본인 | 짧고 얕은 감속. 광역기의 대가다 |
| 탈진 | 탈진 사거리 안의 모두 | 더 길고 깊은 감속 |
| 근접 쿨감 | 술래에게서 일정 거리 안에 있는 **도망자** | 스킬 쿨타임이 빨리 찬다 |

- 광란 누적은 광란이 이미 붙어 있을 때만 쌓인다. 광란이 끊긴 뒤에 잡은 것은 이어 온 추격이
  아니라 새로 시작한 추격이다.
- 근접 쿨감은 도망자에게만 붙는다. 술래 쪽 쿨타임을 빠르게 하면 도망치는 쪽이 위험을 무릅쓸
  이유가 사라진다. 보상이 반대로 가야 붙어 있는 플레이가 성립한다.
- 속도 효과가 겹칠 때 증가군과 감소군을 각각 합산한 뒤 곱한다. 하나의 덧셈으로 합치면 유체화와
  탈진이 정확히 상쇄되어 탈진이 유체화를 "해제"하는 것처럼 보인다.
- 감소에는 바닥값을 둔다. 없으면 효과가 겹칠 때 속도가 0이나 음수가 된다.
- 클라이언트가 받는 것은 "무슨 효과가 걸렸는가"와 "남은 시간 비율"뿐이다. 효과의 크기는 서버만
  안다.

### 2.6 이동과 충돌

- 플레이어는 원, 벽 타일은 사각형으로 다룬다.
- 좌우나 상하가 동시에 눌리면 그 축은 0으로 처리하고, 대각선 이동은 정규화한다. 대각선이 더
  빠르면 안 된다.
- 한 틱 이동량이 크면(유체화 등) 쪼개서 민다. 안 그러면 얇은 벽을 통과한다.
- 플레이어끼리는 서로 통과하지 못한다. 모든 플레이어의 후보 위치를 먼저 계산한 뒤 쌍으로
  해결한다. 한 명씩 순서대로 처리하면 처리 순서가 곧 유불리가 된다.
- 밀어내는 힘은 **접근 속도에 비례한다.** 빠르게 파고든 쪽이 덜 밀리고 상대를 더 민다. 둘 다
  정지해 있으면 절반씩 밀린다.
- 밀어낸 뒤 벽·자기장 충돌을 다시 푼다. 한 번만 돌리면 상대 밖으로 민 결과가 벽 속일 수 있다.
  정해진 횟수만큼 번갈아 반복한다.
- 충돌 계산이 끝난 좌표는 **항상** 맵과 자기장 안의 유효한 값이어야 한다.
- 입력이 없는 플레이어는 정지시킨다. 연결이 끊긴 사람이 마지막 방향으로 계속 가면 안 된다.
- 처리 순서(플레이어, 충돌 쌍, 스킬 요청)는 전부 번호 오름차순으로 고정한다. 결정론이 깨지면
  리플레이가 같은 경기를 재현하지 못하고 회귀 테스트도 짤 수 없다.

### 2.7 맵과 자기장

- 자기장은 맵 바깥에서 안쪽으로 줄어드는 **단단한 사각형 경계**다. 피해 구역이 아니라 벽이다.
  그래서 "자기장 밖" 상태가 존재하지 않고, 이탈 경고도 피해 표시도 필요 없다.
- 자기장이 줄면서 플레이어와 겹치면 안쪽 유효 위치로 밀어낸다.
- 자기장은 연속적으로 줄어든다. 단계도 카운트다운도 없다. 현재 크기는 경과한 틱 수만으로
  계산된다.
- 자기장과 벽이 너무 가까워져 통로가 막히는 경우, 맵 빌더가 **미리** 그 벽이 부서지는 시각을
  계산해 정적 파일에 넣는다. 런타임에서 자기장 주변 벽을 매 틱 검색하지 않는다.
- 맵의 모든 변화는 tick별 타임라인으로 미리 공유된다. 서버 연산과 트래픽을 줄이기 위해서다.
- 맵을 빌드한 틱 규칙과 서버의 틱 규칙이 다르면 같은 타임라인이 다른 속도로 재생된다. 두 값이
  다르면 서버가 뜨지 않아야 한다.
- 맵은 자기장이 끝까지 줄어들면 경기가 끝나도록 만든다. 안 끝나는 맵은 잘못 만든 맵이다.

### 2.8 시야와 은신

시야 판정은 **서버가 한다.** 보이지 않는 플레이어는 좌표를 아예 보내지 않는다. 클라이언트가
받아 놓고 안 그리는 방식은 메모리를 열면 다 보이므로 금지한다.

- 시야는 원이 아니라 화면 비율(16:9)의 사각형이다. 원으로 하면 화면 구석에 있어야 할 플레이어가
  사라진다.
- **벽은 시야를 막지 않는다.** 벽 뒤라도 시야 범위 안이면 보인다. 가리는 것은 수풀과 연막이다.
  벽 차폐를 넣으면 은신 시스템과 역할이 겹친다.
- 은신: 몸이 **완전히** 수풀·연막 안에 들어가야 숨은 것이다. 한 픽셀이라도 밖으로 나오면
  노출이다. 맵 밖은 은신 타일이 아니므로 경계에 붙어서는 숨을 수 없다.
- 근접 노출: 숨어 있어도 아주 가까이 붙으면 보인다. 수풀에 들어간 순간 무적이 되면 안 된다.
- 수풀·연막 안에 있는 플레이어 자신은 시야가 좁아진다.
- 검열 없는 상태를 받는 것은 관전자뿐이다. 살아 있는 플레이어에게는 어떤 경우에도 보내지 않는다.
  이 규칙 하나가 뚫리면 시야 시스템 전체가 무의미해진다.

**관전자가 다 본다는 것은 받아들인 위험이다.**

탈락한 사람은 즉시 관전자가 되고 맵 전체를 본다. 두 사람이 짜고 한 명이 일부러 먼저 죽으면,
그때부터 통화로 술래 위치를 불러 줄 수 있다. 이건 막지 않기로 했다.

막으려면 탈락자의 시야를 살아 있을 때처럼 제한하거나 전체 관전을 몇 초 지연시켜야 하는데,
둘 다 **정상적으로 죽은 사람에게 불쾌한 경험**을 준다. 죽고 나서 남은 판을 구경하는 것이
이 게임에서 진 사람에게 남는 유일한 재미다. 짧고 캐주얼한 게임이라 담합을 막는 값이
정상 사용자가 치르는 값보다 비싸다고 판단했다.

대신 위험이 커지지 않게 두 가지를 지킨다.

- **관전할 수 있는 사람은 그 경기에서 탈락한 사람뿐이다**(§5.3). 경기 중에 방에 들어오는 것은
  되지만 그 사람은 대기실 화면만 본다. 그래서 외부인을 관전자로 불러들일 수 없고, 담합하려면
  **자기 경기를 버려야 한다.** 그 대가가 이 위험의 상한이다.
- **리플레이는 여전히 경기가 끝난 뒤에만 열린다**(§7.2). 관전은 방에 들어와 자리를 차지해야
  하고 방장이 강퇴할 수 있지만, 리플레이 링크는 방 밖 아무에게나 뿌릴 수 있다. 둘은 다른
  위험이라 다르게 다룬다.
- **시야 판정은 한 곳에만 있어야 한다.** 서버의 스냅샷 검열과 리플레이의 개인 시점 재구성이 같은
  함수를 쓰고, 그 함수는 소켓·렌더러·방 객체를 모르는 순수 함수여야 한다. 두 벌이 되면 갈라지는
  순간 과거 리플레이가 거짓이 된다. 판정 내용이 바뀌면 버전을 올린다.

# 서버

## 3. 구성과 신뢰 경계

### 3.1 구성

| 프로세스 | 하는 일 | 개수 |
| --- | --- | --- |
| 매칭 서버 | 인증, 계정, 방 배정, 전적·리플레이 저장, 신고·제재, 운영 API | 1 |
| 게이트웨이 | 외부에 노출되는 유일한 주소. 연결을 해당 인게임 서버로 넘긴다 | 1 |
| 감독자 | 부하를 보고 인게임 서버 프로세스를 늘리고 줄인다 | 1 |
| 인게임 서버 | 방 소유, 대기실, 게임 루프, 판정, 스냅샷 | N |

- 서버 간 메시지는 Redis를 쓴다.
- 방은 생성된 순간부터 종료될 때까지 하나의 인게임 서버 프로세스가 소유한다. 대기실도 인게임
  서버가 관리한다.
- 인게임 서버는 프로세스마다 고유한 id를 가지며 주기적으로 자기 부하를 보고한다. 보고에는
  방 수, 연결 수, 루프 지연, 버전, 그리고 종료 준비 중인지가 들어간다.
- 매칭 서버는 그 보고를 보고 부하가 적은 서버에 방을 만든다. 보고가 끊겼거나, 종료 준비 중이거나,
  클라이언트와 버전이 맞지 않는 서버에는 새 방을 배정하지 않는다.
- 게이트웨이는 정적 설정을 갖지 않는다. 어디로 넘길지는 부하 보고를 보고 정한다. 감독자가
  프로세스를 늘리고 줄여도 게이트웨이를 고칠 일이 없어야 한다.
- **게이트웨이는 그 부하 보고를 그대로 믿지 않는다.** 주소의 형식과 허용 범위, 숫자 값, 갱신
  시각을 검증하고 통과하지 못한 항목은 넘길 곳 목록에서 뺀다. 주소를 읽다 실패해도 프로세스가
  죽으면 안 된다 — 게이트웨이는 외부에 보이는 유일한 주소라 여기가 죽으면 전체가 죽는다.
- 게이트웨이가 그대로 넘겨주는 경로는 필요한 메서드만 받고 본문을 거절한다. 뒤쪽 서버의 응답에도
  제한 시간을 둔다.
- 감독자는 늘리고 줄이는 기준을 **넉넉히 벌려 둔다.** 붙여 두면 늘렸다 줄였다를 반복하는데,
  축소는 진행 중인 방이 끝나기를 기다려야 해서 그 왕복의 대가가 특히 비싸다.
- 사용자는 서버가 몇 대인지, 자기가 몇 번 서버에 붙었는지 알 필요가 없다. 외부에 보이는 주소는
  하나다.
- 인게임 서버 프로세스가 죽으면 그 방들은 끝난다. 다른 프로세스로 옮기지 않는다. 클라이언트는
  매칭 화면으로 돌아간다.
- 프로세스를 내리거나 배포할 때는 먼저 종료 준비 상태로 바꿔 신규 방을 받지 않게 하고, 기존
  방이 모두 끝난 뒤에 내린다.

### 3.2 신뢰 경계 (다른 모든 절보다 우선)

| 주체 | 신뢰 | 이유 |
| --- | --- | --- |
| 클라이언트 | 안 함 | 조작 가능하다. 입력 의도만 받는다 |
| 매칭 서버 | 함 | 신원의 유일한 판정자 |
| 인게임 서버 | 함 | 게임 판정의 유일한 판정자 |
| Redis | 반만 | 내부망 전용. 뚫리면 방 배정과 전적이 통째로 위조된다 |
| 리버스 프록시 | 함 | TLS 종료와 클라이언트 IP 판정 |

- 신원(누구인가)은 **매칭 서버만** 결정한다. 인게임 서버는 클라이언트가 보낸 신원 필드를 어떤
  경우에도 읽지 않는다. 화면에 뜨는 이름은 티켓에 실려 온 값이다.
- 게임 판정(좌표, 속도, 쿨타임, 술래, 탈락)은 **인게임 서버만** 결정한다.
- 인게임 서버는 JWT 비밀키를 갖지 않는다. 인게임 프로세스 한 대가 뚫려도 토큰을 위조할 수
  없어야 한다.
- 계정 상태(정지·탈퇴)는 매칭 서버가 배정 시점에 확인한다.
- 서버는 클라이언트에 계정 식별자를 내려보내지 않는다. 방 안에서 사람을 가리키는 값은 방
  범위의 번호뿐이다.
- 경기 결과는 경험치와 전적에 직결된다. 결과를 쓸 수 있는 주체가 곧 전적을 위조할 수 있는
  주체다. 인게임 서버로 제한하고, 매칭 서버는 자신이 발급하지 않은 경기의 결과를 버린다.
  참가자·경기 시간·킬 수가 배정 시점의 명단과 상한을 벗어나면 저장하지 않고 이상으로 기록한다.
- **경기 식별자는 매칭 서버만 만든다.** 인게임 서버가 스스로 만들 수 있으면 위의 "발급하지 않은
  결과는 버린다"가 무의미해진다. 자세한 규칙은 §5.3을 본다.

### 3.3 접속

- 인증 없이 인게임 서버에 붙는 경로는 **어떤 경우에도** 만들지 않는다.
- 방에 들어가려면 매칭 서버에서 **일회용 접속 티켓**을 받아야 한다. 티켓 하나가 곧 "이 사람이 이
  방의 이 자리에 앉을 권한"이며 유출되면 계정 사칭과 같다.
- 티켓은 추측할 수 없는 난수여야 한다. 방 번호나 시각을 해시한 값처럼 만들어낼 수 있는 값을
  쓰지 않는다.
- 티켓은 **한 번만** 쓸 수 있고 짧게 만료된다. 인증에 성공하면 즉시 폐기하며, 조회와 폐기는
  한 번의 원자적 연산으로 한다. 티켓 하나로 두 연결이 붙는 경합이 없어야 한다.
- 티켓을 발급한 서버, 그 방의 존재, 그 자리의 예약, 같은 계정의 다른 활성 연결 여부를 모두
  확인한다. 하나라도 어긋나면 거절한다.
- **실패 응답은 사유와 무관하게 같아야 한다.** 응답 내용이나 걸린 시간으로 티켓의 존재 여부가
  새면 안 된다.
- 티켓은 주소나 쿼리 스트링에 넣지 않는다. 접속 후 첫 메시지로만 전달한다.
- 연결 시작 시 출처를 검사한다. 허용 목록에 없으면 거절한다. 브라우저의 동일 출처 정책이
  WebSocket에는 적용되지 않으므로, 다른 사이트가 사용자의 브라우저로 우리 서버에 연결을 열 수
  있다.
- 인증 전 연결은 IP당 개수를 제한하고, 정해진 시간 안에 인증하지 않으면 끊는다. 인증 전에는
  인증 메시지 외에 **어떤 메시지도 파싱하지 않는다.**
- 프로세스 전체 동시 연결 상한을 둔다. 상한에 닿은 것은 부하 보고에 반영한다.
- 클라이언트 IP는 리버스 프록시가 넣은 값을 쓰되, **신뢰하는 프록시에서 온 요청에 대해서만**
  그 값을 인정한다. 프록시를 거치지 않는 경로가 열려 있으면 IP를 위조할 수 있고, 레이트리밋이
  IP 기반이므로 그대로 실제 위험이 된다.

## 4. 계정

### 4.1 인증

- **회원가입, 비밀번호 재설정, 계정 삭제는 이메일 인증을 필수로 한다.**
- **로그인한 상태에서의 비밀번호 변경은 현재 비밀번호로 다시 확인한다.** 메일 인증을 걸지
  않는다. 세션이 탈취된 상황에서 공격자가 막히는 것은 "현재 비밀번호를 모른다"는 사실이지
  메일이 아니고, 변경할 때마다 메일을 왕복시키면 정작 급히 비밀번호를 바꿔야 하는 사람이
  느려진다. 비밀번호를 **잊어버려서** 재설정하는 경로가 메일 인증을 쓰는 자리다.
- 비밀번호를 바꾸면 다른 기기의 세션을 모두 폐기한다. 바꾸는 이유의 대부분이 "누가 내 계정에
  들어온 것 같다"이기 때문이다.
- 비밀번호는 8~20자, 영문·숫자·`!@#$%^&*`만 쓴다.
- 인증은 JWT를 쓰되, 세션을 **목록으로 보여주고 하나씩 취소할 수 있어야** 한다. 다른 기기를
  로그아웃시킬 수 있다. 그래서 세션의 권위 저장소는 관계형 DB다.
- 여러 기기에서 동시에 로그인할 수 있다. 새 기기 로그인이 기존 기기를 조용히 밀어내지 않는다.
- refresh token은 원문을 저장하지 않고 해시만 저장한다. DB가 유출돼도 세션을 탈취당하지 않아야
  한다.
- refresh는 쓸 때마다 회전한다. **이미 무효화된 토큰이 다시 오면 탈취로 보고** 그 계정의 모든
  세션을 무효화하고 재로그인을 요구한다.
- **무효화된 토큰으로 새 회전을 해 주지 않는다.** 탭 여러 개가 동시에 갱신하는 정상 경합은,
  처음 회전한 결과를 짧게 보관했다가 **같은 응답을 다시 주는 것**으로 푼다. 옛 토큰에 새 세션을
  내주면 탈취 탐지가 정확히 뒤집힌다 — 훔친 토큰을 반복해서 내밀어 최신 세션을 계속 빼앗고
  정상 사용자를 계속 로그아웃시킬 수 있게 된다.
- access token은 짧게 유지하되, **폐기가 즉시 반영되어야 한다.** 로그아웃·정지·탈퇴·비밀번호
  변경 뒤에는 이미 발급된 토큰도 거절한다. 만료를 기다리는 시간이 곧 제재가 없는 시간이다.
- 계정 정지·탈퇴 시 상태 변경과 세션 삭제를 같은 트랜잭션에서 한다.
- 세션에 남기는 IP는 장기 보관용 HMAC과 짧게 보관하는 암호화 원본으로 나눈다. 단순 해시는
  익명화가 아니다 — IPv4는 전수 대입으로 복원된다. **이 형태는 첫 행을 쓰는 순간 사실상 고정되니
  나중으로 미루지 않는다.**
- 끝난 세션 행도 일정 기간 뒤에는 정리한다. refresh 회전 때문에 활성 사용자 한 명이 하루에
  수십~수백 행을 만든다.

### 4.2 2차 인증

사용자가 켜고 끌 수 있다. **1.0.0 필수다.**

2차 인증은 §4.1의 세션 관리를 대신하지 않고 그 위에 얹는 층이다. 아래 셋이 먼저 성립해야
2차 인증을 붙일 의미가 있다 — 세션 목록과 기기별 로그아웃, 폐기된 refresh 토큰 재사용 시 계정
전체 세션 폐기, 비밀번호 변경·정지·탈퇴 시 발급된 토큰 즉시 거절.

**수단**

- 메일 인증과 OTP(구글 OTP 같은 표준 시간 기반 코드) 중 사용자가 고른다.
- OTP 비밀값은 암호화해서 저장한다. 화면에 다시 보여주지 않는다 — 등록할 때 한 번뿐이다.

**신뢰 기기**

- 신뢰할 수 있는 기기가 아니면 로그인 시 2차 확인을 요구한다.
- 신뢰 기기 등록에는 만료가 있다. 영구 등록은 두지 않는다.
- 등록된 기기를 목록으로 보여주고 하나씩 해제할 수 있다. 세션 목록과 같은 이유다 — 잃어버린
  기기를 사용자가 직접 끊을 수 있어야 한다.

**복구**

- 일회용 백업 코드를 발급한다. 쓴 코드는 다시 쓸 수 없고, 남은 개수를 보여주며, 다시 발급할 수
  있다. 백업 코드는 비밀번호처럼 해시로 저장한다.
- **메일 인증만으로 2차 인증을 통째로 우회할 수 없다.** 복구 절차가 곧 계정 탈취 경로다. 메일이
  뚫린 사람을 지키자고 켠 것을 메일로 풀어 주면 켠 의미가 없다.

**끄는 것도 2차 인증을 요구한다.** 이게 없으면 탈취자가 로그인하자마자 꺼 버린다. 같은 이유로
2차 인증을 켠 계정은 비밀번호 재설정에도 2차 확인을 요구한다.

2차 인증 실패도 레이트리밋 대상이다(§9). 코드를 무한히 넣어 볼 수 있으면 6자리는 금방 뚫린다.

### 4.3 로그인 화면

- 로그인 유지(20일)를 선택할 수 있다.
- 아이디 저장(만료 없음)을 선택할 수 있다. 저장하는 것은 아이디뿐이고 비밀번호나 토큰이 아니다.

### 4.4 게스트

- 로그인하지 않아도 게임을 할 수 있다. 통계·전적·리플레이는 제공하지 않는다.
- 게스트도 정식 사용자와 **완전히 같은 경로로만** 인게임 서버에 접근한다.
- 닉네임은 서버가 `Guest_` + 6자리 무작위 문자로 정한다. 클라이언트가 지정할 수 없다. 혼동하기
  쉬운 문자(0/O, 1/I/l)는 제외한다. 이 형식은 아래 닉네임 규칙의 유일한 예외이며(`_`를 쓴다),
  게스트임이 이름만 보고 드러나야 하므로 정식 사용자는 이 형식을 쓸 수 없다.
- 게스트 식별자는 정식 계정 id와 겹치지 않는 공간을 쓴다. 전적 집계에 섞이면 사고가 난다.
- 게스트 토큰은 짧게 살고 갱신할 수 있다. 갱신은 **같은 게스트 신원을 이어 줄 뿐** 계정 권한을
  주지 않는다. 한 판 하는 동안 튕기지 않게 하려는 것이고, 브라우저를 닫으면 끝난다.
- 게스트 토큰은 계정 토큰과 **다른 키로 서명하고 다른 곳에 보관한다.** 게스트 토큰이 새어도
  계정으로 넘어갈 수 없어야 한다. 게스트 쪽 보관은 계정 쪽(httpOnly 쿠키)보다 약하므로,
  그 차이가 계정 권한으로 이어지지 않는 것이 이 규칙의 전부다.
- 게스트는 신원을 얼마든지 새로 발급받을 수 있다. 그래서 게스트에게 더 좁은 한도를 주는 것은
  방어가 되지 않는다. 대신 **신원 발급 자체에 IP당 제한**을 건다. 신원을 무한히 찍어낼 수
  없어야 §9의 IP 버킷이 의미를 갖는다.

### 4.5 닉네임

- 2~12자, 한글·영어·숫자만. 다른 사용자와 겹칠 수 없다.
- 하한이 2자인 것은 표시 문제가 아니라 사용성 문제다. 한글 두 글자 이름을 막을 이유가 없다.
- 상한 12자는 게스트 닉네임(`Guest_` + 6자 = 12자)과 같은 길이다. 두 종류를 한 벌의 칸 설계로
  담을 수 있어야 한다.
- **클라이언트는 한글 12자가 들어가는 폭으로 칸을 잡는다.** 한글 12자는 영문 12자보다 두 배
  가까이 넓다. 로비 카드, 결과창, 관전 목록, 신고 화면이 모두 이 폭을 견뎌야 한다. 넘칠 때
  줄이는 것이 아니라 처음부터 그 폭으로 만든다.
- **닉네임은 가입할 때 정하고 바꿀 수 없다.** 이름이 바뀌면 신고와 제재가 사람을 놓친다 —
  신고 분류에 '닉네임'이 있다는 것 자체가 이름이 제재 대상이라는 뜻인데, 지적당한 사람이 이름을
  바꿔 빠져나갈 수 있으면 그 분류가 의미가 없다. 나중에 변경을 허용한다면 이력을 남기고 과거
  기록이 옛 이름을 유지하는 방식이어야 한다.
- 금칙어를 미리 거르지 않는다. 사전 필터는 우회가 쉽고 멀쩡한 이름을 막는 대가가 크다. 문제
  있는 이름은 신고로 받아 제재한다.

## 5. 방

### 5.1 만들기와 참가

**만들기**

- 방 이름(1~20자)과 4~8자리 숫자 비밀번호를 설정할 수 있다. 비밀번호는 선택이다. 숫자로
  제한하는 것은 모바일에서 숫자 키패드로 입력하고 구두로 전달하기 쉽기 때문이다.
- 비밀번호 원문은 방을 소유한 인게임 서버 메모리에만 둔다. 방 목록에는 "비밀번호 있음"만 나간다.
  비교는 상수 시간 비교로 한다.
- 방 이름은 다른 사용자 화면에 그대로 표시된다. 매칭 서버가 길이와 문자를 검증하고 제어 문자를
  제거한 뒤 저장한다.

**참가**

- 방 id로 참가하거나, 목록에서 고르거나, 빠른 참가를 쓴다.
- 방 id는 사람이 입력하고 구두로 전달하는 값이라 **6자리** 대문자·숫자 코드로 한다. 혼동하기
  쉬운 문자는 뺀다. 8자리보다 오타가 적고 화면에 크게 띄울 수 있다. 무작위 대입은 참가 빈도
  제한이 막는다. 내부 식별자는 별개이며 사용자에게 노출하지 않는다.
- id로 참가할 때는 우선 id만 요구한다. 비밀번호가 걸린 방이면 그때 비밀번호를 요구한다.
- 방이 꽉 찼으면 들어갈 수 없다.
- **경기 중인 방에도 들어갈 수 있다.** 자리가 있으면 받는다. 다만 진행 중인 경기에는 낄 수 없고
  대기실에서 기다린다. 자세한 것은 §5.3을 본다.
- **실패 사유로 방의 존재 여부가 새면 안 된다.** "없는 방"과 "비밀번호 틀림"은 클라이언트에 같은
  오류로 내려간다.

**빠른 참가**

비밀번호가 없는 방 중에서 다음 우선순위로 고른다.

1. 사람이 충분히 있는 방
2. 대기 중인 방
3. 부하가 적은 인게임 서버의 방

참가 가능한 방이 없으면 새로 만든다. 이름은 `{닉네임}'s Room`으로 자동 지정한다.

**목록**

- 페이지당 **6개**. 모바일 세로 화면 한 장에 들어가는 수이고, 이보다 늘리면 스크롤과 전송량만
  늘어난다.
- 방 이름, id, 비밀번호 여부, 인원 수, 상태(대기 중/게임 중)를 보여준다.
- 목록은 살아 있는 서버가 소유한 방만 보여준다. 프로세스가 죽어 남은 유령 방이 목록에 보이면
  안 된다.
- 훈련장은 목록에도 빠른 참가에도 나오지 않는다.

### 5.2 남용 방지

- **스스로 나갔으면** 같은 방에 **1분간** 다시 들어갈 수 없다. 들어왔다 나가기를 반복하는
  공격은 이 규칙 하나로 끊긴다. 실수로 나간 사람은 1분을 기다린다.
- **강퇴당했으면 그 방이 살아 있는 동안 다시 들어올 수 없다.** 1분이 아니다. 방장이 문제 인원을
  실제로 쫓아낼 수 있어야 다른 방어들이 최후 수단으로 남는다.
- 한 사용자는 동시에 한 방에만 있을 수 있다. 두 번째 배정은 거절하고 기존 방 정보를 돌려준다.
- 방 참가 요청 자체에 빈도 제한을 건다. 방을 바꿔 가며 돌아다니는 경우를 막는다.
- 위 제한은 매칭 서버가 1차로 막고, 인게임 서버가 마지막으로 한 번 더 막는다. 기록이 유실돼도
  방 단위에서는 막혀야 한다.
- 제한은 프로세스 메모리가 아니라 재시작을 견디는 곳에 둔다. 재시작으로 제한이 통째로 풀리면
  안 된다.
- 제한에 걸린 요청은 남은 대기 시간과 함께 거절한다. 이유를 안 알려주면 정상 사용자가 고장으로
  오해한다.

### 5.3 생명주기

한 방은 **대기실 → 인게임 → 결과창**을 반복한다. 게임이 끝났다는 이유로 방이 닫히지는 않는다.

```text
생성 중 ─> 대기실 ─> 시작 ─> 인게임 ─> 결과창
              ^                            │
              └────────────────────────────┘
        대기실에서 인원이 0이면 방을 닫는다
```

- 상태 변경은 전부 서버가 검증한다. 클라이언트가 상태를 지정할 수 없다.

방이 끝나는 경우는 셋이고, 서로 다르다.

| | 언제 | 무엇을 하는가 |
| --- | --- | --- |
| 정상 종료 | 대기실에서 인원이 0이 됐다 | 방을 닫고 자원을 푼다 |
| 유휴 만료 | 사람은 있는데 정해진 시간 동안 상태가 바뀌지 않았다 | 남은 사람을 내보내고 닫는다 |
| 서버 사망 | 방을 소유한 프로세스가 죽었다 | 방이 사라진다. 사람들은 매칭 화면으로 돌아간다 |

유휴 만료를 두는 이유는, 어떤 이유로든 전이가 멈춘 방이 프로세스 자원을 영구히 붙잡고 있으면
안 되기 때문이다. 대기실에 사람이 앉아만 있는 것은 정상이므로 만료 시간은 넉넉해야 한다 —
이건 사람을 쫓아내는 장치가 아니라 고장 난 방을 걷어내는 장치다.

**대기실**

- 방장은 맵을 바꾸고, 방장을 넘기고, 강퇴하고, 게임을 시작하고, 신규 참가를 잠글 수 있다.
- 방장이 나가면 남은 인원 중 가장 먼저 들어온 사람에게 넘어간다.
- 모든 플레이어는 자기 이동기와 번호를 바꿀 수 있다. 번호는 1~8 중 비어 있는 것만 고를 수 있다.
  서버는 요청자가 자기 번호만 바꾸는지, 그 번호가 비었는지 확인한다.
- 다른 플레이어의 닉네임, 간단한 통계, 현재 기기(PC/모바일), 이동기, 번호를 볼 수 있다.
- 준비 상태는 없다. 대신 **누가 막 들어왔으면 5초, 맵이 바뀌면 10초 동안 시작 버튼이 잠긴다.**
  들어온 사람이 이동기를 고를 시간이다. 나가는 것은 잠금을 걸지 않는다.
- 잠금은 서버가 판정한다. 버튼을 우회해 시작 요청을 보내도 거절된다.
- 반복 입퇴장으로 시작을 영원히 막을 수 없어야 한다. 참가로 인한 잠금에는 일정 시간 창 안의
  누적 상한을 둔다. 맵 변경 잠금은 방장만 걸 수 있으므로 상한에서 제외한다.
- 경기가 시작되면 명단·맵·규칙을 잠근다. **잠기는 것은 그 경기이지 방이 아니다** — 이후에도
  자리가 있으면 방에는 들어올 수 있고, 들어온 사람은 다음 경기를 기다린다(§5.1, 아래 인게임 절).

**인게임**

- 서버는 1초에 60틱으로 게임을 처리한다. 클라이언트는 **누른 버튼만** 보내고 서버가 판정한다.
  클라이언트가 보낸 좌표·속도·이동거리·쿨타임은 신뢰하지 않는다.
- **경기 중에도 방에는 들어올 수 있다.** 자리가 남아 있으면 받는다. 다만 진행 중인 경기의
  명단·맵·규칙은 시작할 때 잠겼으므로 그 경기에는 끼지 못하고, **대기실에서 다음 경기를
  기다린다.** 경기가 끝나 대기실로 돌아오면 그때부터 정식 참가자다.
- **경기 중에 들어온 사람은 관전할 수 없다.** 대기실 화면만 본다.

  관전은 검열 없는 화면이기 때문이다(§2.8). 밖에서 들어와 볼 수 있게 두면 아는 사람을 불러들여
  술래 위치를 알려 주는 것이 아무 대가 없이 가능해진다. 방에 들어오는 것 자체는 막을 이유가
  없다 — 다음 경기를 기다리는 사람에게 문을 닫을 필요는 없으니, 문은 열어 두고 화면만 닫는다.
- **관전자가 되는 경로는 하나뿐이다 — 그 경기에서 탈락한 플레이어.** 담합하려면 자기 경기를
  버려야 한다는 뜻이고, 그게 이 규칙이 지키려는 유일한 대가다.
- **관전 자격은 서버가 판정한다.** 클라이언트가 관전 모드를 선언할 수 없다.
- 대기실에 있는 사람에게는 게임 상태를 아예 보내지 않는다. 검열본도 보내지 않는다.
- 관전자는 입력과 스킬 요청을 보낼 수 없다. 보내면 무시하고 남용으로 집계한다. 이모지는 된다.
- 누가 검열 없는 상태를 받는지는 **한 곳에서만** 판정하고, 전송 직전에 그 결과를 참조한다.
  판정이 여러 곳에 흩어지면 한 곳만 빠뜨려도 시야가 샌다.

**결과창**

- 승리자, 각 플레이어의 통계·이동기·번호·닉네임·간단한 통계·현재 기기를 보여준다.
- **10초 후** 자동으로 대기실로 돌아간다. 방장은 그대로 유지되고, 다음 경기는 방장이 다시
  시작한다. 결과를 다시 보고 싶으면 전적 화면에 남아 있으므로, 결과창을 오래 붙잡아 두는 것보다
  다음 판으로 빨리 돌아가는 편이 낫다.
- 재경기에 별도 합의 절차를 두지 않는다. 같은 명단, 같은 방장으로 대기실에 돌아온다.
- 대기실로 돌아갈 때 탈락자와 관전자를 모두 일반 참가자로 되돌린다.

**경기 식별자는 매칭 서버만 만든다**

방 하나가 여러 경기를 치르므로 경기마다 새 식별자가 필요하다. 그것을 인게임 서버가 만들면
발급 주체가 뒤집혀서, 침해된 인게임 서버가 새 식별자로 전적과 경험치를 무한히 적립할 수 있다.

- 첫 경기의 식별자는 방을 배정할 때 매칭 서버가 준다.
- **다음 경기의 식별자는 매칭 서버가 결과를 저장한 직후에 만들어 내려보낸다.** 인게임 서버는
  받은 것만 쓰고, 스스로 만들지 않는다.
- 발급이 아직 오지 않았으면 경기를 시작할 수 없다. 이때는 영구 거절이 아니라 **"잠시 뒤 다시"**로
  답한다. 결과가 저장되는 중이라는 뜻이고, 결과 전송이 밀렸을 때와 같은 상황이다.
- 발급 하나로 경기 하나만 치른다. 한 번 쓴 것은 소모된다.
- 훈련장은 결과를 내지 않으므로 발급도 받지 않는다. 이 규칙은 경기 방에만 건다. 훈련장에까지
  걸면 두 번째 훈련을 시작할 수 없다.

### 5.4 연결이 끊기면

- 즉시 그 플레이어의 입력을 중립으로 만든다. 마지막 방향으로 계속 가면 안 된다.
- 경기 중 살아 있던 플레이어였다면 **그 경기에서는 아웃 처리한다.**
- **5초간 자리와 번호를 보존한다.** 그 안에 돌아오면 같은 번호로 들어오고, 경기 중이면 관전으로
  붙는다. 5초가 지나면 퇴장 처리한다.
- 보존 중인 자리는 다른 사용자에게 배정되지 않는다. 보존 중 같은 계정으로 새 연결이 들어오면
  기존 것을 끝내고 새 연결로 대체한다.
- 재접속은 매칭 서버를 거쳐 **새** 일회용 티켓을 받는다. 인게임 서버에 직접 붙는 경로는 만들지
  않는다. 쓰던 티켓을 재사용하게 만들면 유출된 티켓이 그 시간 동안 계속 유효해진다.

## 6. 게임 루프와 전송

### 6.1 루프

한 프로세스의 스케줄러 하나가 활성화된 모든 방을 돈다. 고정 시간 간격으로 처리하며, 순서는
다음과 같다.

1. 받은 입력 확정
2. 효과 만료와 쿨타임 회복
3. **스킬 판정** (스위치·이동기·이모지)
4. 맵 타임라인의 이번 틱 변경 적용
5. 자기장 계산
6. 상태 효과에 따른 속도와 후보 위치 계산
7. 벽·자기장 충돌 해결
8. 플레이어 간 충돌 해결
9. 7~8을 정해진 횟수만큼 반복
10. 술래 접촉 판정 (그 틱에 생긴 충돌 쌍으로)
11. 생존 상태와 종료 조건 갱신
12. **이번 틱의 권위 프레임 확정**
13. 연결별 시야와 은신 계산
14. 보낼 차례면 연결별 상태 생성

**스킬은 이동보다 먼저 판정한다.** 유체화를 누른 그 틱부터 빨라져야 눌렀을 때 반응한 느낌이
난다. 이동 뒤로 미루면 한 틱씩 늦게 걸려서, 벽 앞에서 쓰거나 잡히기 직전에 쓰는 것처럼
한 틱이 결과를 가르는 상황에서 손해가 난다.

효과 만료와 쿨타임 회복이 그보다 먼저인 이유도 같다. 이번 틱에 쿨타임이 도는 스킬은 이번
틱에 쓸 수 있어야 한다.

**12번의 권위 프레임은 연결을 모르는 단일 상태다.** 13번 이후가 그 프레임에서 연결별 뷰를
파생시킨다. 순서를 뒤집어 "연결을 돌면서 상태를 계산"하는 구조로 만들면 안 된다. 이유는 둘이다.

- 관전자와 플레이어가 같은 틱에 대해 다른 뷰를 받는데, 원본이 하나여야 둘이 어긋나지 않는다.
- 리플레이 기록은 이 경계에 소비자를 하나 더 다는 것으로 끝난다. 특정 연결에 실제로 보낸 것을
  복사해서는 안 된다 — 연결마다 검열도, 기준도, 생략된 프레임도 다르다.

**밀렸을 때 틱을 건너뛰지 않는다.** 따라잡기 횟수에 상한을 두고, 넘으면 남은 시간을 버린다.
틱은 맵 타임라인과 자기장의 기준이라, 틱을 건너뛰면 같은 경기가 서버 부하에 따라 다르게
진행된다. 시간을 버리면 경기가 아주 조금 느려질 뿐 내부 일관성은 유지된다.

지속적으로 루프가 밀리면 그 서버는 신규 방을 받지 않는다.

### 6.2 전송

- 고빈도 상태는 바이너리로, 저빈도 명령과 이벤트는 JSON으로 보낸다. 형식의 정의는 `shared/`에
  한 벌만 둔다. 서버 송신, 개발 샌드박스, 튜토리얼, 리플레이가 모두 같은 함수를 쓴다.
- 클라이언트가 보내는 것은 **현재 눌린 버튼 상태**뿐이다.
- **모르는 것은 건너뛸 수 있어야 한다.** 상태 형식은 길이가 붙은 조각들의 나열이고, 모르는
  조각은 길이만큼 건너뛴다. 서버가 새 정보를 추가해도 구버전 클라이언트가 죽지 않아야 한다.
  한 번 쓴 조각 번호는 의미를 바꿔 재사용하지 않는다.
- **보낸 것이 곧 보이는 것이다.** 목록에 없는 플레이어는 화면에서 사라진다.
- JSON 이벤트는 연출과 알림 용도다. 실제 상태 변화는 다음 상태 전송에도 반드시 반영한다. 이벤트
  하나를 놓쳤다고 클라이언트의 게임 상태가 영구히 달라지면 안 된다.
- 서버는 요청에 성공 응답을 따로 보내지 않는다. 성공은 뒤따르는 상태로 확인하고, 거부만 명시적으로
  응답한다.
- 느린 클라이언트에 과거 상태를 쌓지 않는다. 버퍼가 차면 교체 가능한 위치 정보는 생략하고,
  누적되어야 하는 변경(타일 파괴 등)은 생략하지 않고 다음 패킷에 합친다. 너무 뒤처지면 전체
  상태를 다시 보내고, 한계를 오래 넘으면 연결을 끊는다.
- 작은 패킷에는 압축을 쓰지 않는다.

### 6.3 입력 검증

- 바이너리 패킷은 길이가 정확히 일치할 때만 처리한다. 모자라거나 남으면 통째로 버린다.
- JSON은 아는 형식에 맞을 때만 처리한다. 필수 항목이 빠지면 거부하고, 모르는 항목은 무시한다.
- 열거값(이모지, 맵, 스킬 슬롯, 번호)은 서버가 가진 허용 목록으로 검사한다. 받은 정수를 그대로
  배열 첨자로 쓰지 않는다.
- 지난 입력과 순서가 뒤집힌 입력은 무시한다. 순서 번호가 한 바퀴 도는 것을 고려한 비교를 쓰고,
  그 비교 함수는 한 곳에만 둔다.
- 클라이언트가 보낸 표시 이름은 받지 않는다.
- **사용자 입력을 처리하는 모든 경로에 최종 예외 경계를 둔다.** 잡은 예외는 그 연결만 끊고
  신호로 남긴다. 경계가 없으면 논리 오류 하나가 프로세스를 죽이고, 그러면 그 프로세스가 들고
  있던 **다른 방까지 전부** 같이 죽는다. 연결 하나의 문제가 연결 하나로 끝나야 한다.

### 6.4 클라이언트 예측

서버 권위 모델은 예측 여부와 무관하다. 서버가 언제나 실제 위치와 충돌 결과를 정한다.

- 자기 캐릭터를 서버 응답 전에 미리 움직여 그리는 것은 **표시일 뿐**이며, 설정으로 켜고 끌 수
  있다.
- 예측 결과는 게임 판정에 쓰이지 않고 다른 플레이어에게 전송되지도 않는다.
- 예측을 쓰려면 서버가 마지막으로 처리한 입력 번호를 알려줘야 한다.

## 7. 통계·전적·리플레이

### 7.1 통계와 전적

**통계** — 승률, 스위치 성공률, 평균 생존시간, 판당 평균 킬 수, 게임 수, 경험치, 레벨.

- 레벨은 저장하지 않는다. 누적 경험치의 함수다. 저장하면 곡선을 바꾸는 순간 이미 저장된 레벨이
  전부 거짓이 된다.
- 경험치는 경기 참가·승리·아웃시킨 수·스위치 성공·생존 시간에서 나온다. 생존 시간에는 상한을
  둔다. 없으면 "안 끝나는 방에서 가만히 서 있기"가 최고 효율이 된다.
- 게스트는 전적 저장 대상이 아니다. 경기 기록에는 남되 계정 통계에는 반영하지 않는다.

**전적** — 결과 창에서 볼 수 있는 정보만 저장하며 **30일** 보관한다.

- 경기 단위와 참가자 단위를 나눠 저장한다. 한 덩어리로 뭉치면 나중에 경기 단위 조사와 재집계가
  불가능하다.
- 경기 기록에는 당시 닉네임과 번호, 그리고 버전 도장(빌드·프로토콜·규칙·맵)을 함께 남긴다.
  나중에 컬럼을 추가해도 그 이전 경기는 영원히 빈다.
- 같은 경기가 두 번 저장되지 않는 것은 애플리케이션 로직이 아니라 DB 제약으로 보장한다.

### 7.2 리플레이

서버에 **2시간** 저장되고, 지나면 자동으로 삭제된다.

- **진행 중인 경기의 리플레이는 어떤 경로로도 접근할 수 없다.** 경기가 끝난 뒤에만 열린다.
  이 규칙이 무너지면 리플레이 링크가 실시간 맵핵이 된다.
- 열람 권한은 그 경기의 참가자에게만 준다. 게스트는 계정이 없으므로 열람 권한도 없다.
- 저장소 키나 장기 인증 정보를 브라우저에 노출하지 않는다. 서버가 중계하거나 짧게 만료되는
  주소를 발급한다.
- 운영 조사용 열람과 일반 열람은 권한을 분리하고, 조사 열람은 감사 로그에 남긴다.

**무엇을 기록하나**

- 기록 대상은 루프 12단계의 **권위 프레임**이다. 특정 연결에 보낸 것을 복사하지 않는다.
- 상태 형식은 실시간과 같은 것을 쓴다. 인코더와 디코더가 한 벌뿐이라 형식이 갈라져 리플레이만
  깨지는 일이 없다. 리플레이 플레이어는 곧 기존 엔진이다.
- 시뮬레이션 틱 전부가 아니라 전송 주기로 기록한다. 사람이 본 것은 그쪽이다.
- 보는 사람에 따라 달라지는 값(은신 여부, 수풀 투명도, 내가 누구인지)은 권위 상태가 아니므로
  기록하지 않는다. 재생할 때 시야 판정을 다시 돌려 만든다.
- 다만 **서버가 그때 실제로 내린 시야 판정도 함께 남긴다.** 시야 판정이 바뀌면 과거 경기의 개인
  시점이 그때 실제로 보였던 화면과 달라지는데, 신고 조사에서 이건 치명적이다. 재생 시 다시
  계산한 결과와 비교해 어긋나면 사용자에게 알린다.
- **기록 실패는 경기를 중단시키지 않는다.** 버퍼 한도를 넘거나 쓰기가 실패하면 그 경기의
  리플레이를 포기한다. 경기가 리플레이보다 우선한다.
- 기록이 게임 루프를 막지 않는다. 압축과 파일 쓰기는 루프 밖으로 넘긴다.
- 버퍼에는 프로세스 단위와 경기 단위 양쪽에 상한을 둔다. 상한 없는 버퍼는 곧 메모리 고갈이다.

**파일 다운로드**

플레이어가 직접 받아 보관할 수 있다. 파일에는 다음이 들어간다.

- 게임 버전(빌드, 프로토콜, 규칙, 맵, 시야 판정 버전)
- 맵
- 플레이어 정보(경기 당시 닉네임과 번호)
- **공식 서버에서 만든 파일임을 확인할 수 있는 서명**

- 서명이 증명하는 것은 "이 바이트가 서버에서 나온 뒤 변하지 않았다"는 것뿐이다. 그 사람이 치트를
  쓰지 않았다는 뜻이 아니다.
- 파일에 계정 식별자, 참가하지 않은 사람의 정보, IP, 내부 안티치트 신호를 담지 않는다. 파일은
  언젠가 공유되고, 한번 박히면 되돌릴 수 없다.
- **재생기는 신뢰할 수 없는 입력을 받는다고 가정한다.** 조각 수, 압축 해제 후 크기, 문자열
  길이에 상한을 두고 위치와 길이를 매번 검증한다. 지원하지 않는 버전은 부분 재생을 시도하지
  말고 오류로 끝낸다.
- **상한은 넘는 순간 걸려야 한다.** 다 풀어 놓고 길이를 재면 압축 폭탄은 이미 메모리를 다 쓴
  뒤다. 파일 크기는 읽기 전에 보고, 압축은 상한을 넘는 순간 중단한다. 조각마다 그 조각이 스스로
  선언한 크기를 상한으로 쓰면, 작게 적고 크게 부풀리는 파일은 자기 선언에 걸린다.
- 파일이 적어 온 값(참가자 목록, 버전, 개수)은 화면에 쓰기 전에 형식을 확인한다. 표시가
  escape된다고 해서 형식이 틀린 값이 안전한 것은 아니다 — 화면이 깨진다.

**재생**

- 일시정지, 배속, 프레임 단위 이동, 시간·틱 단위 탐색을 지원한다.
- 탐색은 목표 지점 이전의 가장 가까운 전체 상태에서 시작해 그 뒤만 다시 적용한다. 처음부터
  재생하지 않는다.
- 이모지·점멸 잔상 같은 연출의 수명은 실제 시각이 아니라 재생 시각을 따라야 한다. 엔진에 시간
  소스를 주입할 수 있게 만들면 실시간과 리플레이가 같은 코드로 돈다.
- 탐색 직후에는 임시 연출과 카메라 상태를 초기화한다. 안 하면 과거 이모지가 화면에 남는다.

**보존 hold** — 신고가 들어온 경기의 리플레이는 보존 기간과 무관하게 남긴다. 사라지면 안 되는
것은 화면이 아니라 증거다. 삭제 직전에 hold를 한 번 더 확인한다. 조회 시점과 삭제 시점 사이에
신고가 들어올 수 있다.

## 8. 신고·제재·운영

**이 게임에는 채팅이 없다.** 서로에게 보낼 수 있는 것은 정해진 이모지뿐이다. 글로 욕할 통로를
아예 두지 않는 것이 가장 싼 방어라서 그렇게 정했다. 그래서 '욕설' 신고가 가리키는 것은 닉네임과
이모지 도배이지 대화 내용이 아니다.

- 신고는 **끝난 경기**를 대상으로 만든다. 서버가 아는 경기만 신고할 수 있고, 그 경기의 참가자만
  신고할 수 있다. 진행 중에는 신고를 받지 않는다 — 결과창에서 바로 신고할 수 있으므로 급할
  것이 없고, 경기 중 참가자 판정을 따로 만들면 그만큼 틀릴 자리가 늘어난다.
- **계정만 신고할 수 있다. 게스트는 신고하지 못한다.** 게스트 신원은 얼마든지 새로 만들 수
  있어서, 허용하면 그 자체가 보복 신고 도구가 된다.
- 신고에는 대상 플레이어, 분류(치트/욕설/고의 방해/닉네임), 설명, 가리킨 시점을 받는다.
- 신고가 들어오면 즉시 그 경기의 리플레이에 보존 hold를 건다.
- **신고는 전적이 남아 있는 동안(30일) 받는다.** 다만 리플레이는 2시간이면 사라지므로, 그
  뒤의 신고에는 영상 증거가 없고 설명과 통계만 남는다. 그래서 화면은 **경기 직후에 신고하는
  것이 증거를 남긴다**는 것을 사용자에게 알려야 한다. 이 두 기간을 맞추지 않은 이유는, 신고
  창을 2시간으로 좁히면 나중에 알아차린 사람이 신고할 길이 없어지기 때문이다.
- 신고 접수 자체는 제재 사유가 아니다. 반복·보복 신고 패턴도 함께 집계한다.
- 제재는 최소 두 종류가 필요하다. **계정 정지**(로그인 자체를 막는다)와 **경기 제한**(로그인은
  되지만 경기를 막는다). 둘은 다른 처분이므로 하나의 상태로 뭉뚱그리지 않는다.
- 제재 적용과 취소가 같은 계정에서 겹쳐도 어긋나지 않아야 한다. 취소 하나가 아직 유효한 다른
  제재까지 풀어버리면 안 된다.
- **탈퇴로 제재를 지울 수 없다.** 제재를 받은 계정이 탈퇴하면 이메일을 되돌릴 수 없는 값으로
  바꿔 남기고, 가입할 때 그 값과 대조해 막는다. 방법과 한계는 §14.1을 본다.
- 운영자도 DB를 직접 고치지 않는다. 모든 제재와 해제는 서비스 API를 지나고 감사 로그를 남긴다.
- 운영자 화면은 권한이 있는 계정만 접근할 수 있다.

**IP 취급** — IP는 개인정보이면서 오탐이 많은 신호다. PC방·학교·회사·공유망 때문에 같은 IP가
같은 사람을 뜻하지 않는다.

- 프록시가 검증한 실제 IP만 신뢰한다. 임의의 헤더 값을 저장하지 않는다.
- 원본은 짧게 암호화 보관하고 운영 화면에서는 기본적으로 가린다. 장기 상관관계는 HMAC으로만
  본다.
- **이 규칙은 저장되는 모든 곳에 똑같이 적용된다.** 세션 테이블만이 아니라 감사 로그, 신고
  기록, 제재 기록, 애플리케이션 로그, 백업까지다. 한 곳이라도 평문으로 남으면 나머지를 아무리
  잘 다뤄도 소용이 없다. 특히 "부가 정보"를 통째로 JSON으로 담는 컬럼이 이 규칙을 조용히
  빠져나가기 쉬우니, IP가 흘러드는 경로를 주기적으로 다시 훑는다.
- 원본 열람은 권한과 사유를 요구하고 감사 로그에 남긴다.
- **IP 단독으로 영구 제재하지 않는다.** 공격을 멈추기 위한 짧은 제한에 우선 쓴다.

**안티치트** — 서버가 위치·속도·충돌·시야·쿨타임을 판정하므로 전통적인 속도핵·좌표 변조는
구조적으로 막힌다. 안티치트는 그 권위 모델을 대신하지 않고 경계에서 나오는 이상 징후를 모은다.

- 위반은 감지한 자리에서 로그를 찍지 말고 **하나의 신호로 만들어 단일 지점으로 넘긴다.** 초기
  소비자가 로그 하나뿐이어도 된다. 중요한 것은 감지 지점이 소비자를 모른다는 점이다. 나중에
  저장소나 운영 화면을 붙일 때 흩어진 로그 스무 군데를 찾아다니지 않아도 된다.
- 신호에는 심각도와 **규칙 버전**을 넣는다. 탐지 기준이 바뀐 뒤에도 과거 판단을 설명할 수 있어야
  한다.
- 명백한 프로토콜 위반은 연결 종료나 짧은 자동 제한을 할 수 있지만, 행동 통계와 IP 상관관계는
  사람의 검토 큐로 보낸다.

## 9. 레이트리밋

- 로그인 여부와 상관없이 IP와 브라우저 정보 등을 조합해 건다.
- **기능별로 다르게** 건다. 등급은 다음과 같다.

| 등급 | 대상 | 기준 |
| --- | --- | --- |
| 아주 좁게 | 로그인, 회원가입, 비밀번호 재설정, 메일 발송, 신고 | 분당 한 자릿수 |
| 좁게 | 방 생성, 방 참가, 빠른 참가 | 분당 10~20회 |
| 보통 | 전적·프로필 조회 | 분당 수십 회 |
| 넓게 | 상태 확인, 공개 설정 조회 | 분당 100회 이상 |

- **게스트와 계정에 다른 숫자를 주지 않는다.** 한 기능에는 한도 하나다. 예전에는 게스트를 더
  좁게 잡았는데, 게스트는 신원을 얼마든지 새로 만들 수 있어서 그 숫자가 애초에 방어가 아니었다.
  숫자만 둘로 늘어나고 지키는 것은 없었다.
- **실제로 세는 것은 IP다.** 신원은 만들면 되지만 IP는 그렇지 않다. 인증 관련 경로에서 IP 버킷은
  로그인 여부와 무관하게 항상 센다. 신원이 붙었다고 IP 세기를 그만두면 "분당 5회"가 "신원당
  분당 5회"가 되고, 그건 제한이 없는 것과 같다.
- **신원 버킷도 함께 센다. IP 대신이 아니라 같이 센다.** 계정 하나가 여러 IP를 옮겨 다니며
  두드리는 것은 IP 버킷에 걸리지 않고, 반대로 PC방·학교처럼 한 IP 뒤에 여러 사람이 있는 곳에서는
  IP 버킷만으로 막으면 애먼 사람이 같이 막힌다. 두 버킷은 서로의 빈틈을 메운다. NAT를 감안해
  IP 한도는 신원 한도보다 넓게 둔다.
- **브라우저 정보는 보조 신호로만 쓴다.** 같은 IP 뒤의 여러 사람을 갈라 보는 데 도움이 되지만,
  마음먹은 공격자는 값을 바꾸면 그만이라 이것만 믿을 수 없다. **지문을 따로 만들어 저장하거나
  쌓아 두지 않는다** — 막지도 못하면서 개인정보만 하나 더 늘리는 쪽이 되기 때문이다. 판단은
  그 요청을 처리하는 동안에만 하고 남기지 않는다.
- **대상 단위 버킷이 필요하다.** 한 계정을 여러 IP에서 두드리는 것은 IP 버킷으로 막히지 않는다.
  로그인 실패와 인증 메일 발송은 대상 계정·주소 기준으로도 센다.
- 인증 실패도 세야 한다. 잘못된 토큰이 검증에서 먼저 걸러져 카운터를 지나치지 않게 한다.
  검증 비용 자체가 공격 대상이다.
- 카운터는 프로세스 메모리가 아니라 재시작을 견디는 곳에 둔다. 서버를 여러 대로 늘려도 총
  한도가 늘어나면 안 된다.
- 인게임 연결에도 상한이 있다. 프레임 크기, 초당 입력 패킷 수, 초당 명령 수, 이모지 간격, 방
  비밀번호 시도 횟수. 위반 집계는 연결뿐 아니라 사용자와 IP 단위로도 유지해 재접속으로
  초기화되지 않게 한다.

## 10. 저장소

- 세션과 전적은 관계형 DB에 둔다.
- Redis는 서버 검색, 방 검색, 배정 명령, 결과 전달, 그리고 인증 쪽의 임시값(메일 인증 코드,
  레이트리밋 카운터)에만 쓴다. **게임 쪽 용도와 인증 쪽 용도는 키 공간을 분리한다.**
- **플레이어 위치 같은 틱 상태는 Redis에 쓰지 않는다.**
- 매칭 서버용과 인게임 서버용 접근 권한을 분리한다. 인게임 서버 계정은 자기 키와 결과 전달,
  방 목록에만 접근할 수 있어야 한다.
- 환경 구분(dev/staging/prod)을 키 생성 지점에서 강제한다. 구분 없는 직접 접근을 코드에서
  금지한다.
- 살아 있음을 나타내는 키에는 만료를 건다. 프로세스가 죽으면 등록부에서 스스로 사라져야 하고,
  마지막 갱신 시각만 보고 판단하게 두지 않는다.
- 모든 스트림에 길이 상한을 건다. 상한이 없으면 소비자 장애가 그대로 메모리 고갈이 된다.
- 결과 전달은 유실되면 안 된다. 처리 결과를 쓴 뒤에 원본을 확인 처리한다. 순서를 바꾸면 결과가
  사라진다.
- 같은 명령이 두 번 도착해도 부수 효과 없이 같은 답을 돌려준다. 이미 포기한 요청이 뒤늦게
  처리되어 유령 방이 남지 않도록, 명령에는 만료 시각을 넣는다.
- **Redis가 잠시 죽어도 진행 중인 방의 게임 루프는 계속 돌아야 한다.**

## 11. 트래픽과 성능

- 시뮬레이션은 60Hz, 상태 전송은 그보다 낮은 주기로 한다. 판정 주기와 전송 주기를 같게 두면
  전송량이 두 배가 된다.
- **보이지 않는 플레이어는 보내지 않는다.** 안 보내는 것이 곧 안 보임이다.
- 매 틱 바뀌지 않는 값(닉네임, 맵)은 매 틱 보내지 않는다.
- 8인 풀방에서 한 사람당 하행은 10 KB/s를 넘지 않는 것을 목표로 한다. 넘기는 변경은 근거를
  남긴다.
- 느린 클라이언트가 다른 클라이언트나 서버 루프를 지연시키지 않아야 한다.
- 프로세스당 방 수 상한은 추정하지 않고 **측정해서** 정한다. 8인 풀방으로 틱 처리 시간과 루프
  지연을 재고, 방을 늘려가며 목표 틱 레이트가 무너지는 지점의 절반을 상한으로 잡는다.
- 측정 전에는 **보수적인 기본값**을 쓴다. 기본값이 있다는 것과 확정했다는 것은 다르다 — 설정
  파일의 값에 "측정으로 나온 값이 아니다"를 남겨 두고, 측정이 끝나면 그 주석을 지우면서 확정한다.
- 측정은 재현할 수 있어야 한다. 어떤 기계에서, 몇 분 동안, 무엇을 기준으로(p95·p99) 쟀는지를
  결과와 함께 남긴다. "측정했다"만 남으면 다음 사람이 다시 잰 값과 비교할 수 없다.
- **1.0.0은 자동 확장에 기대지 않는다.** 감독자가 프로세스를 늘리고 줄이는 기능은 있지만,
  출시 시점의 용량은 측정으로 확정한 고정 용량으로 잡는다. 부하가 그 선을 넘으면 늘리는 것이
  아니라 새 방을 거절하는 쪽이 안전하다.

# 클라이언트

## 12. 클라이언트

라이트/다크 테마를 지원하고 모바일과 PC를 모두 지원한다. React SPA이지만 URL로 화면을 구분한다
(`/login`, `/lobby`, `/game`, `/result` 등).

**지원 범위**

| | 기준 |
| --- | --- |
| 데스크톱 | Chrome·Edge 103+, Firefox 113+, Safari 16.4+ |
| 모바일 | iOS Safari 16.4+, Android Chrome 103+ |

이 선이 취향이 아니라 기능에서 나온 이유는 리플레이 재생 때문이다. 브라우저가 제공하는 압축
해제(`DecompressionStream`)를 쓰는데 Safari는 16.4에서 들어왔다. 그 아래에서는 리플레이만
조용히 깨진다.

- **지원하지 않는 브라우저에는 접속 시 그렇게 알린다.** 게임이 반쯤 동작하다 이상한 곳에서
  깨지는 것보다, 들어오는 순간 "이 브라우저는 지원하지 않는다"고 말하는 쪽이 낫다.
- 세로 화면과 가로 화면을 모두 견딘다. 인게임은 가로를 기준으로 하되 세로에서도 조작할 수 있어야
  한다.

### 12.1 디자인

- 컬러 팔레트는 `client/src/theme/color.ts`를 따른다.
- Jalnan은 큰 글씨, Gmarket Sans는 작은 글씨에 쓴다.
- 둥글둥글하고 가득 찬 느낌으로 만든다. 텍스트를 너무 작게, 너무 많이, 부연설명을 길게 적지
  않는다. **AI처럼 적지 않는다.**
- **잘 보이는 것이 예쁜 것보다 앞선다.** 둘이 부딪히면 가독성을 고른다. 조금 덜 예뻐도 읽히는
  쪽이 낫다. 특히 **인게임처럼 빠르게 지나가는 화면은 한눈에 들어와야 한다** — 거기서 놓친
  정보는 다시 볼 기회가 없다.

  이건 방향이지만 판정할 수 있는 선을 함께 둔다. 취향으로 다투지 않기 위해서다.

  - 글자와 아이콘은 배경과 충분히 갈려야 한다. 본문 4.5:1, 큰 글씨와 UI 요소 3:1을
    기준으로 삼는다(WCAG 2.2). 파스텔을 쓰더라도 **채움에만** 쓰고, 읽어야 하는 글자와
    뜻을 지는 테두리는 이 선을 넘겨야 한다.
  - **화면에 실제로 찍히는 크기로 잰다.** 설계 캔버스의 숫자가 아니다. 1920 캔버스를
    축소해 쓰는 구조라 두 값이 크게 다르고, 작은 화면일수록 벌어진다. 폰 가로처럼 가장
    불리한 화면에서도 읽을 수 있어야 한다.
  - 월드 위에 얹는 글자(닉네임, 번호)는 배경이 매번 바뀌므로 색만으로는 부족하다.
    외곽선이나 받침을 둬서 어떤 바닥 위에서도 읽히게 한다.
- 라이트 테마는 진한 테두리에 안쪽을 연하게 채우고, 다크 테마는 네온사인처럼 안쪽이 투명하고
  테두리만 남는다.
- 버튼·슬라이더 등 상호작용 요소는 마우스가 닿으면 살짝 커지고 색이 진해진다.
- 인게임을 제외한 화면은 Tab으로 포커스를 옮길 수 있어야 한다.
- 아래에 언급되지 않은 화면은 이 철학에 맞게 만든다.

### 12.2 인게임 엔진

**엔진은 판정하지 않는다.** 누가 숨었는지, 누가 누구를 볼 수 있는지, 수풀이 얼마나 비치는지를
엔진이 계산하는 코드는 존재해서는 안 된다. 엔진은 서버가 내려준 결과만 그린다.

| 사실 | 누가 정하나 |
| --- | --- |
| 이 플레이어가 보이는가 | 서버. 안 보내면 화면에 존재하지도 않는다 |
| 보이지만 수풀 속인가 | 서버가 보낸 플래그 |
| 수풀이 얼마나 비치나 | 서버가 보낸 값 |
| 자기장 범위, 이펙트 잔여시간 | 서버 |
| 줌 | **클라이언트** |

줌만 예외인 이유는, 보면 안 되는 플레이어가 애초에 패킷에 없어서 줌아웃으로 얻을 것이 없기
때문이다. 그래서 서버 왕복 없이 즉각 반응하게 둔다.

엔진은 하나지만 진입 모드가 셋이다.

| 모드 | 자기 자신 | 데이터 출처 |
| --- | --- | --- |
| 플레이 | 있음 | 서버 |
| 관전 | 없음 | 서버(검열 없음) |
| 도움말·튜토리얼 | 가상 | **로컬 스크립트. 서버 없음** |

튜토리얼이 서버 없이 도는 것이 핵심이다. 같은 엔진에 같은 형식의 상태를 로컬에서 만들어 먹인다.
즉 **튜토리얼 스크립트 = 미니 서버**이고, 리플레이 플레이어도 같은 구조의 미니 서버 하나다.
덕분에 전용 렌더링 분기가 필요 없다.

**React와 렌더러의 경계는 "화면 고정이냐 월드 고정이냐" 하나다.** 맵·플레이어·이펙트·머리 위
이모지는 월드 쪽, 목록·생존자 수·스킬 버튼·이모지 선택 휠·알림·메뉴는 화면 쪽이다. 페이지가
양쪽을 따로 조립하면 상태가 갈라지므로 바깥에 노출하는 것은 컴포넌트 하나뿐이다.

**매 틱 바뀌는 값을 React 상태로 올리지 않는다.** 좌표를 올리면 초당 수십 번 리렌더가 돈다.
HUD는 쿨다운·생존자 수처럼 사람이 읽는 저빈도 값만 구독한다.

**탭이 안 보이는 동안 흐른 시간을 따라잡지 않는다.** 브라우저는 보이지 않는 탭의 타이머를 1초
이상으로 묶고 프레임 요청은 아예 멈춘다. 그동안 밀린 시간을 돌아온 순간에 몰아서 처리하면
화면이 갑자기 빨라지거나 캐릭터와 카메라가 튄다.

- 시간을 세어 진행하는 것(도움말 데모, 리플레이 재생)은 **숨으면 멈추고 보이면 그 지점부터
  이어 간다.** 안 보이는 화면을 그릴 이유도 없다.
- 한 프레임에 들어온 경과 시간에는 상한을 둔다. 위치 보간과 카메라 연출도 같은 상한을 쓴다.
  하나만 묶고 나머지에 원래 값을 넘기면 그 부분만 튄다.
- **경기 중에는 밀린 것을 클라이언트가 따라잡지 않는다.** 판정은 서버가 하고 클라이언트는 받은
  최신 상태를 그린다. 돌아오면 그 상태부터 이어 그리면 된다.
- 남은 시간을 **목표 시각에서 계산하는** 타이머(결과창 카운트다운, 시작 잠금)는 원래 이 문제가
  없다. 그런 것은 그대로 둔다.

### 12.3 조작

**키보드** — 이동, 이동기, 스위치 지목(1~8), 이모지.

- 모든 키를 최대 2개까지 설정할 수 있다.
- 같은 키를 두 기능에 배정할 수 없다.
- 테스트 기능으로 키가 실제로 눌리는지 확인할 수 있다.

**모바일** — 터치로 같은 것을 다 할 수 있어야 한다.

- 이동은 조이스틱으로 한다.
- **스위치는 번호를 골라야 하는 스킬이라 터치에서도 번호를 고를 수단이 필요하다.** 조이스틱을
  당겨 방향으로 번호를 고르고 떼면 발동하는 방식으로 한다. 손가락이 선택지를 가리면 안 되므로
  선택 UI는 손가락 위가 아니라 화면 가운데에 띄운다. 이모지도 같은 방식이라 배우는 것은 하나다.
- 조이스틱의 위치와 크기는 사용자가 바꿀 수 있다. 손과 화면 크기가 사람마다 다르다.
- 터치 조작을 켤지는 자동 판정하되 사용자가 덮어쓸 수 있다. 터치 노트북, 폰에 붙인 키보드 같은
  조합이 실재한다.

### 12.4 화면

**타이틀** — 타이틀, 게임 시작, 도움말, 테마 전환, 설정, 로그인/계정 정보, 매칭 서버 연결 상태
아이콘(초록·노랑·빨강과 ping).

**설정** — 스크롤 가능하게, 각 항목에 짧은 설명.

- 기본: 테마, 언어(한국어·영어), 개인정보처리방침, 크레딧, 클라이언트 빌드 번호
- 사운드: 마스터·효과음·배경음악 음량, 배경음악 관리. **BGM은 최초 접속 때 받지 않고 사용자가
  켤 때 받는다. 받은 뒤에는 캐싱해서 다시 받지 않는다.**
- 인게임: 프레임, 해상도, 그래픽 품질, 이펙트 정도 / 색약 모드 / 프레임·핑·서버 tps·패킷 로스
  표시
- 키 맵핑: 위 조작 절의 규칙

**도움말** — 필요하면 실제 인게임 엔진으로 짧은 영상처럼 재생한다.

- 기본 게임 소개, 술래 외형, 효과(광란·피곤)의 조건과 이펙트, 스킬, 훈련장 버튼
- 도움말이 적는 숫자는 손으로 찍지 않고 밸런스 파일에서 가져온다. 손으로 찍으면 밸런스를 고칠
  때마다 조용히 거짓말이 된다.

**그 외** — 로그인·가입·비밀번호 재설정, 방 목록·생성·참가, 대기실, 인게임, 결과, 프로필과
전적, 리플레이 재생, 훈련장, 운영자 화면.

### 12.5 표시 설정

인게임 설정은 **전부 로컬 표현 설정이다.** 서버로 나가지 않고, 서버가 보내주는 정보의 양도
바꾸지 않는다. 안 보여야 할 플레이어는 애초에 전송되지 않으므로 설정을 조작해 얻을 것이 없다.

- 해상도 배율을 바꿔도 **보이는 범위는 고정한다.** 해상도로 시야를 넓히는 꼼수가 생기면 안 된다.
- 품질을 낮춰도 **정보는 지우지 않는다.** 수풀·연막 덩어리, 잔여시간, 자기장 경계, 술래 표시,
  이펙트 표시는 최저 품질에서도 남는다. 빠지는 것은 같은 사실을 반복해 말하는 장식뿐이다.
- 모션을 줄여도 **수명은 흐른다.** 진동만 멈추고 이모지·잔상은 제때 사라져야 한다.
- 색약 모드는 화면에 필터를 씌우는 방식이 아니라 **팔레트 자체를 바꾼다.** 필터는 색각이상을
  흉내 내는 것이라 정작 당사자에게는 아무 정보도 주지 않는다. 유형별로 구분 가능한 색을 다시
  고르고, HUD 명단의 색 점도 같은 팔레트를 쓴다. 명단 색과 본체 색이 어긋나면 그 UI의 존재
  이유가 사라진다.
- 빨강(술래·자기장)과 파랑(유체화)은 건드리지 않는다. 위험 신호의 관습이고 모양으로도 뒷받침된다.

### 12.6 언어

한국어와 영어를 지원한다. 새 문구를 추가하면 두 언어에 모두 넣는다. 한쪽만 있는 문구는 버그다.

# 그 외

## 13. 도구

**맵 빌더** — 자기장을 미리 시뮬레이션해서 자기장과 벽이 너무 가까워지면 벽을 부수는 시각까지
정적 파일로 만들어 둔다. 맵의 모든 흐름을 미리 공유해 서버 연산과 트래픽을 줄인다. 가능하면
GUI로 만든다. 만들어진 데이터는 서버 빌드에 포함한다. 런타임에서 원본을 다시 읽지 않는다.

**오프라인 리플레이 플레이어** — 리플레이 파일을 넣으면 재생하는 프로그램.

**자동화 테스트** — 사용자 입장에서 브라우저를 조작해 전체 흐름을 확인하는 테스트.

## 14. 운영과 법무

여기까지가 "무엇을 만드는가"였다면 이 절은 "그것을 사람에게 열어 두는 데 필요한 것"이다.
베타에서는 없어도 됐지만 1.0.0에서는 여기가 사고 지점이다.

### 14.1 데이터 수명과 탈퇴

무엇을 얼마나 들고 있는지가 한 곳에 있어야 한다. 개인정보처리방침이 이 표를 그대로 인용한다.

| 무엇 | 얼마나 | 탈퇴하면 |
| --- | --- | --- |
| 계정(이메일, 비밀번호 해시) | 계정이 살아 있는 동안 | **지운다** |
| 닉네임 | 계정이 살아 있는 동안 | **푼다.** 다른 사람이 다시 쓸 수 있다 |
| 세션 | 만료 뒤 정해진 기간 | 즉시 전부 폐기 |
| 세션 IP | HMAC은 상관관계 기간, 원본은 짧게 | 함께 지운다 |
| 전적(경기·참가자) | 30일 | 계정과의 연결만 끊고 남긴다 |
| 경기 기록 속 당시 닉네임 | 전적과 같다 | **남긴다** |
| 리플레이 | 2시간 (신고 hold는 예외) | 기간대로 사라진다 |
| 신고·제재 기록 | 조사와 회피 방지에 필요한 기간 | 식별자만 남기고 개인정보는 지운다 |
| 제재받은 계정의 이메일 HMAC | 제재가 살아 있는 동안 | **남긴다.** 재가입 차단에만 쓴다 |
| 감사 로그 | 조사에 필요한 기간 | 남긴다. 단 IP는 §8의 규칙을 따른다 |

- **탈퇴는 상태를 바꾸는 것이 아니라 지우는 것이다.** 상태만 `DELETED`로 두고 이메일과 비밀번호
  해시를 계속 들고 있으면, 사용자 입장에서 탈퇴한 것이 아니다.
- 탈퇴한 이메일로 **다시 가입할 수 있어야 한다.** 지운 계정이 유일성 제약에 걸려 재가입을 막으면
  안 된다. **단 제재를 받은 계정은 예외다** — 아래를 본다.

**제재받은 사람의 재가입 차단**

이메일을 지우면서 제재만 남기면, 제재를 받은 사람이 탈퇴하고 같은 주소로 다시 가입해 처음부터
시작할 수 있다. 그러면 §8의 "탈퇴와 재가입으로 제재를 지울 수 없다"가 거짓말이 된다.

- 제재를 받은 계정은 탈퇴할 때 **이메일 원문 대신 `HMAC(서버 비밀키, 정규화한 이메일)`을**
  제재 이력에 남긴다. 원문은 지운다.
- 가입 요청이 오면 입력한 이메일을 같은 방식으로 변환해 그 목록과 대조한다. 걸리면 가입을
  거절한다.
- 세션 IP를 다루는 방식과 같다. **되돌릴 수 없고, 대조하는 데만 쓰고, 목적이 끝나면 지운다.**
  단순 해시가 아니라 HMAC인 이유도 같다 — 비밀키가 없으면 후보 주소를 대입해 맞춰 볼 수 없다.
- 제재를 받지 않고 탈퇴한 사람은 **아무것도 남기지 않는다.** 이 값은 제재 이력이 있는 계정에만
  생긴다.
- 기간이 정해진 제재는 그 기간이 지나면 HMAC도 지운다. 영구 정지만 계속 남는다. 처리방침에
  이 사실을 적는다.
- **이 방어의 한계를 알고 쓴다.** 다른 이메일 주소로 가입하는 것은 막지 못한다. 그건 원래 기술로
  막을 수 없고, 이용약관에서 금지하고 신고·조사로 처리한다. 이 규칙이 막는 것은 "같은 주소로
  아무 일 없었다는 듯 돌아오는 것"뿐이다.
- 거절 화면은 **이유를 구분해 말하지 않는다.** 이미 가입된 주소인지, 제재된 주소인지가 응답으로
  갈리면 남의 가입 여부와 제재 여부를 확인하는 도구가 된다. 화면에는 언제나 같은 문구를 띄우고,
  실제 사정은 그 주소로 보내는 메일에서만 알린다. §5.1에서 방의 존재 여부를 숨기는 것과 같은
  이유다.
- 경기 기록에 남는 "당시 닉네임"은 지우지 않는다. 그 경기가 누구와 누구의 경기였는지가 기록의
  전부라, 지우면 남은 사람들의 전적도 같이 망가진다. 계정과의 연결은 끊는다.
- 제재 이력은 남긴다. 지우면 탈퇴와 재가입이 제재를 지우는 방법이 된다.

### 14.2 이용약관과 개인정보처리방침

- **실제 문서가 있어야 한다.** "준비 중"은 1.0.0이 아니다. 본문은 `legal/privacy-policy.md`와
  `legal/terms-of-service.md`에 있다. 이 두 파일은 설계 문서가 아니라 이용자에게 보여 주는
  내용이라 §0의 "문서 하나" 규칙 밖이다. 화면은 이 파일을 옮겨 담을 뿐 다른 내용을 적지 않는다.
- **§14.1의 표와 두 문서의 보관 기간은 같아야 한다.** 한쪽만 고치면 이용자에게 한 약속과 코드가
  갈라진다. 보존 기간을 바꿀 때는 설정 파일·§14.1·처리방침 셋을 함께 고친다.
- 가입할 때 약관과 처리방침에 동의를 받고, **동의한 시점과 버전을 기록한다.**
- 처리방침에는 최소한 처리 목적, 수집 항목, 보존 기간(§14.1의 표), 파기 절차, 이용자의 권리와
  행사 방법, 문의 창구가 들어간다.
- 방침이 바뀌면 알리고, 바뀐 버전에 다시 동의를 받는다.

**운영 주체**

| | |
| --- | --- |
| 이름 | 0-INF |
| 문의 | zero2inf.zip@gmail.com |
| 저장소 | https://github.com/0-inf |

- 이 이름과 문의 창구를 개인정보처리방침, 이용약관, 크레딧 화면에 표시한다.
- 문의 창구는 **실제로 답이 가는 곳**이어야 한다. 처리방침에 적은 주소로 이용자가 열람·정정·삭제를
  요구할 수 있고, 그 요구에 답할 사람이 있어야 한다.

### 14.3 점검과 공지

- **점검 모드가 있어야 한다.** 서버를 내리기 전에 "지금 점검 중이고 언제 돌아온다"를 보여줄 수
  있어야 한다. 지금은 매칭 서버를 내리면 사용자가 정체불명의 실패만 본다.
- 점검 중에는 새 방과 새 로그인을 막되, **경기 중인 방은 끝나게 둔다.** 이미 §3.1의 종료 준비
  상태가 그 동작이다.
- 공지를 띄울 자리가 있어야 한다. 최소한 타이틀 화면에서.

### 14.4 백업과 복구

- 관계형 DB, Redis, 리플레이 파일 각각에 대해 **백업 주기와 보관 위치**를 정한다.
- 백업은 암호화해서 보관한다. 백업에도 §8의 IP 규칙이 그대로 적용된다.
- **복구를 실제로 해 본 적이 있어야 한다.** 해 보지 않은 백업은 백업이 아니다. 복구 시험은
  주기적으로 하고 결과를 남긴다.
- 어디까지 잃어도 되는지(얼마 전 시점으로 돌아가도 되는지)와 얼마 안에 복구해야 하는지를
  저장소별로 적는다. 계정은 잃으면 안 되고, 리플레이는 2시간짜리라 잃어도 된다 — 이 차이를
  백업 정책에 반영한다.

### 14.5 사고 대응

- 무엇을 보고 있는지 정한다. 최소한 서버 생존, 루프 지연, 오류율, 결과 저장 적체.
- 어떤 값에서 사람을 부르는지 정한다. 임계값이 없으면 아무도 안 본다.
- **개인정보가 새면 알려야 한다.** 무엇이 샜는지, 언제 알았는지, 무엇을 했는지를 정리해 이용자에게
  알리고 필요한 신고를 한다. 그 절차를 미리 적어 둔다 — 사고가 난 뒤에 절차를 만들면 늦는다.
- 사고가 나면 증거부터 보존한다. 로그와 감사 기록을 지우거나 덮어쓰지 않는다.

### 14.6 배포

- 지원하는 배포 형태를 **하나로** 정한다. 지금은 개발용 실행 스크립트만 있다.
- 설정이 빠졌거나 틀리면 **기동을 거부한다.** 조용히 기본값으로 도는 것이 가장 위험하다.
  리플레이 서명 키가 없는데 무서명 파일을 만드는 것이 그 예다.
- DB 스키마 변경은 앞뒤 버전이 함께 도는 시간을 견뎌야 한다. 되돌릴 수 있는 범위를 정한다.
- 비밀값(키, 비밀번호)은 코드와 이미지에 넣지 않고, 바꿀 수 있어야 한다.

### 14.7 저작권

- 폰트, 배경음악, 효과음, 이미지의 **출처와 라이선스를 목록으로 남긴다.** 상업적 이용과 재배포가
  허용되는지 확인한 결과를 함께 적는다.
- 고지가 필요한 자산은 크레딧 화면에 실제로 고지한다. 지금 크레딧 화면은 자리표시자다.
- 저장소 자체의 라이선스를 정한다.

## 15. 1.0.0의 범위

### 15.1 1.0.0에 반드시 있어야 하는 것

이게 없으면 출시하지 않는다.

- §14.2의 이용약관·개인정보처리방침과 동의 기록 (운영 주체는 0-INF로 정해졌다)
- §14.1의 탈퇴 시 실제 삭제
- §4.2의 2차 인증
- §14.3의 점검 모드
- §14.4의 백업과 **복구 시험 1회 이상**
- §14.7의 자산 라이선스 목록
- §11의 용량 측정과 그에 따른 고정 용량 확정
- §16의 점검 기준 전 항목

### 15.2 1.0.0 이후로 미루는 것

경계와 버전 식별자는 지금 넣되 기능은 미룬다. 나중에 끼워 넣으면 구조를 다시 뜯어야 하는 것만
지금 만든다.

| 미루는 것 | 이유 |
| --- | --- |
| 서버 간 메시지 서명과 Redis 권한 분리 | 한 대에서 도는 동안은 한 프로세스가 뚫렸을 때의 수평 이동 문제다. 여러 호스트로 나눌 때 함께 한다 |
| 리플레이 링크 공유·만료·취소 | 웹 리플레이 플레이어가 먼저 있어야 의미가 있다 |
| 운영자 페이지의 역할 분리, 2인 승인, 대시보드 | 실제 운영 인력이 있을 때 필요하다. 지금 필요한 것은 화면이 아니라 기록과 감사 로그다 |
| 안티치트 신호 저장과 통계 탐지 | 관측 전에 만든 탐지 규칙은 오탐만 만든다. 신호를 내보내는 지점만 만들어 둔다 |
| 진행 중인 방을 다른 프로세스로 옮기기 | 초기에는 그 방을 끝내고 매칭 화면으로 돌려보낸다 |
| 닉네임 변경 | §4.5. 허용하려면 이력과 과거 기록 보존을 함께 설계해야 한다 |

### 15.3 만들지 않는 것

방향이 아니라서 안 하는 것들이다.

| | 이유 |
| --- | --- |
| 채팅 | 글로 욕할 통로를 두지 않는 것이 가장 싼 방어다. 이모지로 충분하다 |
| 유료 보관과 분석 | 이 게임이 팔 것이 아니다 |
| 금칙어 사전 필터 | 우회가 쉽고 멀쩡한 이름을 막는 대가가 크다. 신고로 받는다 |
| 랭킹·시즌 | 등수가 없는 게임이다. 통계는 자기 기록을 보는 용도다 |

### 15.4 소급이 불가능해서 지금 넣는 것

나중에 넣으면 과거 데이터가 영원히 비는 것들이다. 이미 다 들어가 있고, 빠지지 않았는지만 본다.

경기 기록의 버전 도장과 당시 닉네임, IP의 분리 저장, 권위 프레임 경계, 시야 판정의 단일화와
버전, 위반 신호의 단일 방출 지점, 경기 식별자의 발급 주체.

## 16. 점검 기준

이 문서로 점검할 때는 아래 순서로 본다. 각 항목은 "그렇다/아니다"로 답할 수 있어야 한다.

### 16.1 보안 (뚫리면 나머지가 의미 없다)

- 클라이언트가 보낸 신원 필드가 서버 어디에서도 쓰이지 않는다.
- 클라이언트가 좌표·속도·쿨타임을 서버에 관철할 방법이 없다.
- 다른 방 또는 다른 서버의 티켓으로 접속할 수 없다.
- 이미 쓴 티켓과 만료된 티켓이 거절되고, 실패 사유가 응답으로 구분되지 않는다.
- 허용 목록에 없는 출처의 연결이 거절된다.
- 인증 메시지를 보내지 않는 연결이 제한 시간 안에 정리된다.
- 방장 전용 요청을 다른 참가자가 보내면 거절된다.
- **살아 있는 플레이어가 검열 없는 상태를 받을 수 없다.**
- **대기실에 있는 사람에게 게임 상태가 전송되지 않는다.**
- 관전자가 보낸 입력과 스킬 요청이 게임에 영향을 주지 않는다.
- **경기 중에 방에 들어온 사람은 관전할 수 없다.** 대기실 화면만 보고, 그 경기의 상태는 검열본도
  받지 않는다. 관전은 그 경기에서 탈락한 사람에게만 열린다.
- 비공개 방의 존재 여부가 오류 응답으로 노출되지 않는다.
- 매칭 서버가 발급하지 않은 경기의 결과가 저장되지 않는다.
- 같은 사용자가 두 방에 동시에 배정되지 않는다.
- 프레임 크기와 메시지 빈도 상한을 넘는 연결이 종료된다.
- 게스트가 신원을 새로 받아도 IP 단위 제한을 우회할 수 없다.
- 게스트의 전적이 저장되지 않고, 게스트가 닉네임을 지정할 수 없다.
- **어느 저장소에도 원본 IP가 평문으로 남지 않는다.** 세션 테이블뿐 아니라 감사 로그, 신고·제재
  기록, 애플리케이션 로그, 백업까지 본다. 특히 부가 정보를 통째로 담는 JSON 컬럼을 훑는다.
- 탈퇴한 계정의 이메일과 비밀번호 해시가 남지 않고, 같은 이메일로 다시 가입할 수 있다.
- **제재를 받고 탈퇴한 계정의 이메일로는 다시 가입할 수 없다.** 그때도 이메일 원문은 남지 않는다.
- 제재를 받지 않고 탈퇴한 사람은 이메일 HMAC도 남지 않는다.
- 가입 거절 화면이 "이미 가입됨"과 "제재됨"을 구분해 보여주지 않는다.
- 2차 인증을 켠 계정은 신뢰하지 않는 기기에서 로그인할 때 2차 확인을 요구받는다.
- 2차 인증을 **끄는 것**과 비밀번호를 재설정하는 것에도 2차 확인이 걸린다.
- 메일 인증만으로 2차 인증을 우회할 수 없다. 백업 코드는 한 번 쓰면 다시 쓸 수 없다.
- 2차 인증 코드 시도에 레이트리밋이 걸려 있다.
- 경기가 시작된 뒤 들어온 사람이 **그 경기의 명단에 끼어들지 못한다.** 잠기는 것은 그 경기의
  명단·맵·규칙이지 방의 문이 아니다(§5.1, §5.3).
- 게스트가 신고를 넣을 수 없다.
- 진행 중인 경기의 리플레이에 접근할 수 없다.
- 폐기된 refresh 토큰으로 새 세션을 받을 수 없고, 그 시도가 계정의 모든 세션을 무효화한다.
- 로그아웃·정지·비밀번호 변경 뒤에 이미 발급된 access token이 거절된다.
- 인게임 서버가 스스로 만든 경기 식별자로는 결과가 저장되지 않는다.
- 사용자 입력 처리 중의 예외가 그 연결만 끊고, 같은 프로세스의 다른 방에 번지지 않는다.
- 게이트웨이가 형식이 틀린 부하 보고를 받아도 죽지 않고 그 서버로 넘기지도 않는다.
- 상한을 넘는 압축 파일이 **다 풀리기 전에** 거절된다.

### 16.2 남용 방지

- 시작 잠금 중 도착한 시작 요청이 거절된다. 버튼 상태와 무관하게 막힌다.
- 참가를 반복해도 잠금 누적 상한을 넘겨 시작을 무한히 지연시킬 수 없다.
- 같은 방을 나간 직후 재참가가 쿨다운으로 거절된다.
- 강퇴당한 사용자가 같은 방에 다시 들어올 수 없다.
- 레이트리밋이 기능별로 다르게 걸려 있고, 재시작으로 초기화되지 않는다.
- 신원을 붙여도 IP 버킷이 사라지지 않는다. 게스트 신원을 늘려 인증 한도를 늘릴 수 없다.
- 게스트와 계정에 서로 다른 한도 숫자가 박혀 있지 않다.
- 브라우저 정보로 만든 지문이 어디에도 저장되지 않는다.
- 매칭 서버를 두 대로 늘려도 같은 총 한도가 유지된다.
- 한 계정을 여러 IP에서 두드리는 것이 대상 단위 버킷에 걸린다.

### 16.3 게임

- 같은 초기 상태와 같은 입력이 항상 같은 결과를 만든다.
- 플레이어가 벽, 자기장, 다른 플레이어를 통과하지 않는다.
- 충돌 계산 후 좌표가 항상 맵과 자기장 안에 있다.
- 자기장 위치와 맵 타임라인이 정확히 일치한다.
- 시야 판정이 서버와 리플레이에서 같은 함수를 쓰고, 그 함수가 소켓·렌더러·방 객체를 모른다.
- 권위 프레임이 연결 수와 무관하게 동일하다. 관전자가 붙거나 떨어져도 플레이어가 받는 내용이
  달라지지 않는다.
- 역할이 바뀌면 쿨타임이 사라진다.
- 스위치 실패도 쿨타임을 소모한다.
- 생존자가 2명 이하가 되면 경기가 끝나고 남은 전원이 승리자다.
- **경기가 끝난 방에서 다음 경기를 시작할 수 있다.** 발급이 아직 안 왔으면 "잠시 뒤 다시"이지
  영구 거절이 아니다. 훈련장은 발급 없이도 반복된다.
- 경기에 최대 시간이 걸려 있고, 도달하면 생존자 전원이 승리자로 끝난다.
- 스킬이 이동보다 먼저 판정된다. 유체화를 누른 그 틱부터 빨라진다.
- 경기 중 연결이 끊기면 즉시 아웃되고, 5초 안에 돌아오면 같은 번호로 관전에 붙는다.
- 리플레이 기록이 실패해도 경기가 정상 진행되고 결과가 저장된다.
- 경기 결과에 버전 도장과 당시 닉네임이 빠짐없이 들어간다.

### 16.4 부하와 전송

- 8인 풀방 하나에서 틱 처리 시간과 루프 지연을 실제로 측정했다.
- 느린 클라이언트가 다른 클라이언트나 서버 루프를 지연시키지 않는다.
- Redis가 멈춰도 진행 중인 방의 게임 루프가 유지된다.
- 종료 준비 중인 서버에 신규 방이 배정되지 않는다.
- 버전이 다른 클라이언트의 접속을 명확히 거부한다.
- 매 틱 바뀌지 않는 값이 매 틱 나가지 않는다.

### 16.5 클라이언트

- 엔진에 시야를 판정하는 코드가 없다.
- 매 틱 바뀌는 값이 React 상태로 올라가지 않는다.
- 인게임 설정이 서버로 나가지 않고, 받는 정보의 양을 바꾸지 않는다.
- 한글 12자 닉네임이 어느 화면에서도 칸을 넘치지 않는다.
- 모바일에서 스위치·이모지를 포함한 모든 조작이 가능하다.
- 두 언어에 문구가 모두 있다.
- 도움말의 숫자가 밸런스 파일에서 나온다.
- 탭을 오래 숨겼다 돌아와도 화면이 몰려 나오거나 캐릭터·카메라가 튀지 않는다.
- 지원하지 않는 브라우저로 들어오면 그렇게 알린다. 반쯤 동작하다 깨지지 않는다.
- 가입 화면에서 약관과 처리방침에 동의를 받고, 그 시점과 버전이 기록된다.
- 개인정보처리방침과 크레딧이 실제 내용이다. 자리표시자가 아니다.

### 16.6 운영

- 점검 모드로 새 방과 새 로그인을 막을 수 있고, 경기 중인 방은 끝까지 간다.
- 백업이 돌고 있고, **복구를 실제로 해 봤다.**
- 설정이 빠지면 서버가 조용히 기본값으로 돌지 않고 기동을 거부한다.
- 무엇을 보고 어떤 값에서 사람을 부르는지 정해져 있다.
- 자산 라이선스 목록이 있고, 고지가 필요한 것은 크레딧에 있다.

---

숫자나 규칙이 이 문서와 코드에서 다르면 **코드가 틀린 것이다.** 코드 쪽이 옳다고 판단되면 이
문서를 먼저 고치고, 그 결정을 남긴다.
