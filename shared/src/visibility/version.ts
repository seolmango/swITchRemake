/**
 * 시야 판정 결과가 달라지는 변경마다 올린다. 상수 튜닝도 결과가 달라지면 포함이다.
 *
 * 이 값은 경기 결과와 리플레이 manifest에 함께 기록된다. 리플레이를 재생할 때 현재 코어 버전과
 * 기록된 버전이 다르면, 재생 화면이 그때 실제로 보였던 화면과 다를 수 있다는 뜻이다.
 * 그래서 이 숫자를 올릴 때는 과거 리플레이가 어떻게 되는지 함께 판단해야 한다.
 */
export const VISIBILITY_CORE_VERSION = 1;
